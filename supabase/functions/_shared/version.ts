/** Single source of truth for the release version reported by every health endpoint. */
export const APP_VERSION = "10.4.0";
/** Newest migration this code requires; the deploy workflow refuses to deploy onto an older schema. */
export const SCHEMA_VERSION = "20260926120000";
