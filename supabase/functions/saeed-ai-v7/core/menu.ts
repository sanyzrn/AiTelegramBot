/** Pure Telegram menu definitions; the single source lives in _shared so the gateway routes the same buttons. */
export {
  ADMIN_PAGES,
  BUTTON_COMMANDS,
  DASHBOARD_BUTTON,
  LANG,
  MENU,
  SIZES,
  TONES,
  TOOLS,
  allButtons,
  keyboard,
  rows,
} from "../../_shared/menu.ts";
export { toneGuide } from "../../_shared/tone.ts";
