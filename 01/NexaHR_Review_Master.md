# NexaHR — 11-Prompt Code Review (Master Report)

Repo: https://github.com/sanyzrn/NexaHR · branch `main` @ `27c83e5` (shallow clone, read-only).
Each of the 11 prompts in `NexaHR_11_review_prompts.md` was executed as its own review. This document
contains (1) how the reviews were run, (2) a cross-review synthesis, and (3) all 11 reviews verbatim,
each in the exact output format its prompt requires.

---

## How this was run (RULE 1 — RUN IT)

| CI command | Result | Where |
|---|---|---|
| backend `ruff check .` + `pytest -q` | **1542 passed, 0 failed** in 7m05s | `/tmp/pytest_backend2.log` |
| launcher `ruff check tools` + `pytest tools/tests -q` | **passed** | `/tmp/launcher.log` |
| frontend `npm run lint` (oxlint) | **0 problems** | `/tmp/fe_lint_test.log` |
| frontend `npm test` (vitest) | **407 passed** | `/tmp/fe_lint_test.log` |
| frontend `npm run build` (tsc -b && vite build) | **green** (main chunk 120.24 kB gzip; recharts chunk isolated) | P10 report |
| `alembic upgrade head` (fresh DB, twice) | **63 migrations, clean**, head `f1b6d3e28a47` | P06 report |
| e2e `bash e2e/run_e2e.sh --api-only` (mock LLM) | **"ALL PASSED"** (P04 agent, live run) | P04 report |

Environment notes and deviations, reported per the prompts' Rule 1:

- **PostgreSQL 16.4 was built from source with ICU** for this review (the sandbox had no Postgres
  server and no sudo; the pip-bundled and conda builds are compiled `--without-icu`, which makes every
  `COLLATE "fa-x-icu"` query fail — 26 tests failed on that build, **all** traced to the missing
  collation and **none** to the repo). With ICU-enabled PG16.4 the full suite is green.
- The prompts' counts are stale relative to `main@27c83e5`: 1542 backend tests (not 1131), 63
  migrations (not 57), 47 registered copilot tools (not 43), 407 vitest tests (not 285). Treated as
  drift, not findings; P04 re-counted the tool table at 47 rows.
- `docs/review-findings.md`, `docs/nafashr-port-candidates.md`, `docs/nafashr-reported-bugs-check.md`
  named by the prompts **do not exist** in this checkout; `docs/open-findings*.md` is present and was
  read by every agent **after** forming its own view, per the prompt's ordering rule.
- Findings that were executed carry that mark in their reports; anything a reviewer could not execute
  is explicitly labelled UNVERIFIED (notably: one P07 crash-window duplicate-send finding).
- Three subagent dispatches for P06 timed out on infrastructure (4 attempts); P06 was executed
  directly by the coordinator agent instead. Its probes are recorded in its report.

---

## Cross-review synthesis

### Convergent findings (independently found by ≥2 reviewers — highest confidence)

1. **List/queue scope binds role→single seat, while guards accept over-rank seats** (P01 #1 Medium,
   P02 #1 Medium, echoed in P04's parity table). `may_act_at` (`workflow.py:696`) deliberately lets a
   higher-rank role occupy a lower seat; `scope_evaluations_for_role` (`evaluations.py:641-646`) and
   every queue/dashboard/copilot copy of it filter by role→one seat field, so a deputy-role user
   sitting legally in a `unit_supervisor` seat can *work* a case he can never *see* in any list.
2. **HR-unit shield is per-endpoint discipline, not a central rule** (P05 #1 Medium; P01 #2 Low;
   P04 context drift): the shield holds on list/detail/writes, but leaks through
   `dashboard.py:575-612` (`personnel/{id}/in-progress`), several personnel/dashboard aggregate
   fields, self-assessment invite (`personnel.py:236-243` writes `invited_at` onto the shielded
   record), and the copilot's `context.py:42-67` people-scope.
3. **Copilot `my_open_cases` queue drift** (P02 #3, P04 parity "NO" rows): HR branch drops the
   own-case exclusion; CEO branch misses `hr_approved`+no-deputy cases that `IS_ON_CEO_DESK` includes.
4. **Copilot context people-scope lost a union its twin got** (P01 #4, P04, P05): `context.py:42-67`
   lacks the record-seat union `tools/people.py:71-79` has — model context is *under*-inclusive.

### Consolidated findings (all severities, all reviews)

| # | Sev | Review | file:line | What breaks |
|---|---|---|---|---|
| 1 | Medium | P01+P02 | `evaluations.py:641-646` (+ `personnel.py:117-121/237-245`, `dashboard.py:751-784`, `ai/tools/evaluations.py:526-551`) | Over-rank seat holder creates/scores/opens a case (201/200) but sees total=0 in every list/queue/dashboard/copilot surface; findable only via notification links |
| 2 | Medium | P05 | `dashboard.py:575-612` | `personnel/{id}/in-progress` has no shield/own-case guard: HR reads id/code/status/was_returned of a shielded open case the list hides and detail 403s |
| 3 | Medium | P08 | `schemas/personnel.py:12` (+ `personnel.py:379,656,76-79` vs `personnel_import.py:105,151,429`) | Manual path accepts Persian-digit personnel_code «۱۲۳۴» verbatim while import normalizes to ASCII → same person can exist twice; codes unsearchable across scripts |
| 4 | Medium | P11 | `org_units.py:138-144` | Rename→personnel cascade has no test; executed mutation (rename moves nobody) survives the whole suite — unit renames would silently strand members from cohorts/shield/analytics |
| 5 | Low | P03 | `snapshot.py:63-75` | Four snapshot selects lack `ORDER BY` → the legal PDF's row order is plan-dependent (executed: index plan vs seq scan give different orders for identical data) |
| 6 | Low | P03 | `frontend/src/components/ScoreForm.tsx:213` | Preview rounds half-up on doubles vs server's Numeric half-away-from-zero: typed 1.005 shows «+۱» on screen, «+۱٫۰۱» on the signed document |
| 7 | Low | P03 | `schemas/evaluation.py:154` → `EvaluationDetailPage.tsx:769` | Read API shows only raw bonus while document/Excel print applied value; at base=100 the document omits the bonus block and its reason (repo's own open item, still reproducing) |
| 8 | Low | P02 | `notifications.py:389-505` (+ `workflow.py:427,442`) | No notification branch for `ceo_return_no_deputy(_hr_subject)` — CEO returns a no-deputy case and nobody is notified (executed: 0 recipients in all 8 shapes) |
| 9 | Low | P01/P02/P04 | `ai/tools/evaluations.py:530-532,549-551` | Copilot `my_open_cases` drifts from UI queues (HR own-case exclusion; CEO `hr_approved` no-deputy leg) |
| 10 | Low | P01 | `dashboard.py:575-612` | Same surface as #2 — shielded case metadata readable (cross-confirmed) |
| 11 | Low | P05 | `personnel.py:236-243/282-303`, `dashboard.py:487-494`, `self_assessment.py:146-204`, `tools/people.py:120-150` | Shielded-case existence/state leaks (open_evaluation_id, self_assessment_state, has_open); invite writes `invited_at` onto the shielded record |
| 12 | Low | P04 | `confirmations.py:104-108` | Confirm-time 403s are unlogged and leave the row `pending` (dead card until TTL) |
| 13 | Low | P04 | `orchestrator.py:247-263` | Risky proposals emit no audit event — unreconstructable after 30-day purge |
| 14 | Low | P04 | `prompt.py:85/88-89` + `orchestrator.py:550` | Injection frame defeatable by 40×U+2500 run in data; tool results unframed (blast radius bounded by guards + confirmation) |
| 15 | Low | P07 | `periods.py:247` + `evaluations.py:452-454` | `close_period` (no FOR UPDATE) races `create_evaluation`'s unlocked period read → a case can be born under a period a concurrent close just shut (executed) |
| 16 | Low | P07 | `self_assessment.py:158/167/181` | Unlocked read-decide-write invite: `invited_at` + audit + "window open" notification can land on an already-closed window (executed) |
| 17 | Low (UNVERIFIED) | P07 | `delivery.py:160-197` + `scheduled.py:583` | Send-before-commit: crash after `channel.send` re-sends SMS/email; "exactly once" overclaims (at-least-once) |
| 18 | Low | P08 | `frontend/src/utils/dates.ts:50-61` (+ `SubmissionDeadlineBar.tsx:41-45`, `jalali.ts:197-200`) | Frontend "today" uses browser-local components, not ORG_TIMEZONE → deadline banner/filter presets disagree with backend around Tehran midnight (executed with frozen clock) |
| 19 | Low | P09 | `OpenCaseCard.tsx:309-314` (+ 4 more fields) | 5 textarea fields have no maxLength/counter vs server caps → submit-time 422 the Persian error can't attribute (20 self-assessment notes context) |
| 20 | Low | P09 | `ui/focusTrap.ts:52-58` | One Escape closes *every* stacked overlay (copilot + modal → half-filled form state lost) |
| 21 | Low | P09 | `ImprovementPlanDetailPage.tsx:253/270-278` (+3) | 5 controls under the repo's own 24px WCAG minimum, invisible to the tap-target test's regex |
| 22 | Low | P09 | `UsersPage.tsx:57` | Employee→personnel link picker silently truncated at 1000 — person #1001 can never get an account link, no hint |
| 23 | Low | P10 | `services/excel.py:157-174` + 5 call sites | `note_truncation` reloads+re-saves the whole workbook for one cell: +122% build time (measured 2036 ms median blocking full export) |
| 24 | Low | P10 | `analytics.py:66-70` | `my-scoring` takes no period/window, re-scans all scores ~2×/view (14 q / 159 ms at 140k scores), linear growth with no cap |
| 25 | Low | P11 | `org_units.py:158-197` | DELETE /api/org-units + in-use guard untested (deleting an occupied unit would go green) |
| 26 | Low | P11 | `notifications.py:82-131` | Preference guards ("email tick without address" 400) deletable by mutation |
| 27 | Low | P11 | `frontend/src/pages/VerifyPage.tsx:25-47` | Public QR-authenticity page has zero tests — always-«معتبر» mutation passed all 407 |
| 28 | Low | P11 | `admin.py:65-97` | Delivery-queue problem list untested (hiding every failed row stays green) |

### Regressions: zero

Every reviewer re-verified the ALREADY-FIXED lists within its angle. **No item on any ALREADY-FIXED
list was found broken.** Two items the prompt listed as known-former-defects were confirmed *fixed in
code*: the `af_race_*` cross-test pollution (cleanup now in `finally`, `test_audit_fixes.py:665-703`)
and the `employee_view_on` module-hiding (all 9 switches have default-off tests + meta-guard).

### What the reviews say is sound (the short version)

Authz core (JWT/`token_version`, refresh rotation+theft detection, lockout atomicity, forced-password
allowlist); the full state machine over all 8 chain shapes (no stuck states, no deputy-bypass of CEO
finalize, all paired invariants agree); score math + signed-document pipeline (redistribution, bonus
double-clamp, scheme stamping, signatories, hash→QR→verify); copilot (47/47 tool parity on guards,
fail-closed registration, confirmation flow survived every executed attack, injection cannot become
silent execution); privacy call sites (every `suppressed_avg` counts people); concurrency core
(FOR UPDATE on every workflow path, audit-chain advisory lock, scheduler leader lock); clock/UTC
boundary (every timestamptz filter through `local_day_start`); frontend cache boundaries (memory-only
token, full clear on login/logout); performance (no N+1 in any read path, every shipped filter
indexed, bundle cleanly split); test suite discipline (zero mock-assertion tests, deterministic time,
committed-row hygiene).

---


# ══════════════════════════════════════════════════════════════════
# PROMPT 01 REVIEW
# ══════════════════════════════════════════════════════════════════

# P01 — Authz & Session review (Task 4-a)

Mode: defects + elevation · HEAD 27c83e5 · backend suite 1542 passed (log /tmp/pytest_backend2.log); launcher 89 passed, frontend 407 passed. Targeted scenarios executed against a private DB `nexahr_p01` (created, migrated, dropped) via `TestClient` + real PostgreSQL — every finding below was executed, none is UNVERIFIED.

## Verdict — 3 lines
- The authz core is genuinely sound: every guard I traced lives on both the HTTP and the copilot path (mostly by delegating to the same endpoint function), JWT/`token_version` revocation, refresh rotation with theft detection, the forced-password allowlist, the DB-backed lockout and the token-gated public surfaces all held up under execution.
- Four real defects, all one family — seat/role visibility drift: an evaluator sitting in a *lower* seat than their role (deliberately supported by `may_act_at`) owns cases that no list, queue, dashboard or copilot surface shows (medium); the HR-unit shield, the HR own-case exclusion, and the personnel-scope union each drift on exactly one surface (3 × low, no other-people data leaks).
- No REGRESSIONs found among the ALREADY FIXED items; the docs the prompt names (`docs/review-findings.md`, `docs/nafashr-port-candidates.md`, `docs/nafashr-reported-bugs-check.md`) do not exist in this checkout — I read `docs/open-findings.md` / `docs/open-findings-ai.md` instead, after forming my view. Design objection (not a bug): `users.update_user` can deactivate/role-change the last active `manage_capabilities` holder — the last-holder guard exists only in `set_capabilities` (administration.py:219-237), and recovery depends on the same actor having `manage_users`.

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. Medium | `backend/app/api/routers/evaluations.py:641-646` (also `personnel.py:117-121` + `:237-245`, `dashboard.py:751-784`, `ai/tools/evaluations.py:526-551`) | An evaluator whose role is *higher* than the seat they occupy sees none of their assigned work in any list/queue.** `may_act_at` (workflow.py:696-715) and seat assignment (evaluation_access.py:40-47) deliberately let a superior sit in a lower seat, but `scope_evaluations_for_role` branches on the role's *single* seat column (deputy → `deputy_user_id == user.id`), as do `_ACCESS_COLUMN_BY_ROLE` (personnel list join), role-overview `mine` conditions (dashboard.py:751-752, 783-784) and `my_open_cases` (tools/evaluations.py:533-543). **Reach (executed):** HR sets person X's chain with `unit_supervisor = deputy-account D` and a *different* deputy in the deputy seat (upsert accepts it); D creates X's evaluation → 201, opens it by id → 200, can score and submit — but `GET /api/evaluations` returns `total=0`, role-overview counters are 0, `GET /api/personnel` omits X while `GET /api/personnel/{X}` is 200, copilot `my_open_cases`/`search_evaluations` are empty. D finds their work only via notification deep-links or by guessing ids. (The CEO-in-supervisor-seat variant is masked only because a single-CEO org also puts them in `ceo_user_id`, which the ceo branch catches.) **Fix:** derive row visibility from the seats the user *occupies* — union `unit_supervisor_user_id == uid | deputy_user_id == uid | ceo_user_id == uid` in the role branches of `scope_evaluations_for_role`, `_ACCESS_COLUMN_BY_ROLE`, `my_open_cases` and role-overview — the same union `_can_view_personnel` (personnel.py:124-156) and `my_scoring_profile` (analytics.py:84-88) already apply.

**2. Low | `backend/app/api/routers/dashboard.py:575-612` | The HR-unit shield has a third surface that never asks about it.** The shield pair (`hr_panel_is_shielded` workflow.py:134-151 / `IS_SHIELDED_FROM_HR_PANEL` workflow.py:154) is applied to the list (evaluations.py:637), the Excel export (evaluations.py:781) and the detail page (`_ensure_can_view` → `ensure_hr_may_handle`, evaluations.py:190) — but `personnel_in_progress` guards only through `_can_view_personnel`, which grants HR unconditionally (personnel.py:137). **Reach (executed):** HR-unit member's own case exists in `draft` (`hr_review_skipped=True`); another HR user: evaluations list → case absent (correct), detail → 403 (correct), `GET /api/dashboard/personnel/{id}/in-progress` → **200** with `evaluation_id`, `evaluation_code`, `status: draft`, `was_returned`. Metadata only (no scores/comments), but it is precisely the "existence + stage" of the in-flight case the shield says HR must not see, and it hands out the record id the detail guard just 404'd. **Fix:** in `personnel_in_progress`, return `null` for an HR viewer when `hr_panel_is_shielded(record)` (the subject keeps their view via `/api/me/evaluations/open`, which already provides it).

**3. Low | `backend/app/services/ai/tools/evaluations.py:530-532` | The copilot queue drops the HR own-case exclusion the HTTP list enforces.** `scope_evaluations_for_role` excludes `subject_personnel_id == user.personnel_id` for HR (evaluations.py:637-640); the `hr` branch of `my_open_cases` selects `status == submitted` with no subject filter. **Reach (executed):** an `hr`-role account linked to personnel *outside* the HR unit; their supervisor submits their case (status `submitted`) → `GET /api/evaluations` excludes it (correct), copilot `my_open_cases` **includes it** (id, evaluation_code, stage, own name); `get_evaluation` on it correctly 403s via `ensure_not_deciding_about_oneself`. Exposure is metadata of one's *own* case only — a two-paths drift, not a leak of others' data. (Shielded cases cannot intersect: every transition into `submitted` has `guard: not skips_hr_review`, workflow.py:184-262.) **Fix:** add the subject exclusion (or reuse the HR branch of `scope_evaluations_for_role`) in `my_open_cases`.

**4. Low | `backend/app/services/ai/context.py:42-67` | The prompt-context twin of `_visible_personnel_ids` lost the record-seat union its sibling gained.** `ai/tools/people.py:46-82` unions `EvaluationRecord` seats into the visible set (the 1.11.1 fix for open-findings-ai item 2, whose docstring says the rule lives in `_can_view_personnel`); `ai/context.py:42-67` — same name, same claimed contract ("`None` یعنی همه — دقیقاً مثل … رابط") — still queries only `EvaluationAccess` seats. The parity test covers only the tools copy (`test_personnel_visibility_scope.py:207-232` asserts `tools_visible`, never `context_visible`). **Reach (executed):** a record with `unit_supervisor_user_id = supA` and no matching access row (chain reassigned after scoring): `tools_visible` contains X, `context_visible` does not — so `get_personnel` (200) shows the person while the model's context omits them and the copilot disclaims knowledge. Under-inclusive, not a leak. **Fix:** have `context.py` import the tools' `_visible_personnel_ids` instead of keeping a second copy, and extend the parity test to assert both functions agree on the record-seat case.

## Verified correct
- **JWT + `token_version` revocation**: `tv` claim compared in `get_current_user` (deps.py:66-67) and in `refresh` (auth.py:205-206); bumped and sessions revoked on self password change (auth.py:277-286), HR reset (users.py:297-304), and separation (personnel.py:615-626); malformed `sub` → 401, not 500 (deps.py:56-61); HS256 with fixed algorithm list (security.py:69-73); production refuses default/short JWT secrets (config.py:214-231).
- **Refresh rotation & theft detection**: `rotate_session` row-locks with `FOR UPDATE` + `populate_existing` (sessions.py:103-108), 60 s grace for double-tab, reuse outside grace revokes the whole family (sessions.py:120-125); `purge_dead_sessions` can only make rejection stricter (sessions.py:141-167); cookie is HttpOnly/SameSite=strict/path-scoped, Secure in production (auth.py:75-86); confirm-claim is an atomic conditional UPDATE (confirmations.py:53-61).
- **Forced password change** is a server-side allowlist (deps.py:17-36, 72-76), deliberately minimal; refresh/logout still reachable for the affected account.
- **Lockout & rate limits**: lock checked before verification and even for nonexistent usernames (auth.py:121-130, 151-162); `record_failure` is atomic upsert + `FOR UPDATE` + `populate_existing` (login_guard.py:76-93); dummy-hash verify equalizes timing for unknown users (auth.py:52, 157); HR notified with a dedup key (login_guard.py:137-160); audited `unlock` endpoint (users.py:318-350); login and change-password share the 10/min ceiling; `/api/verify/{token}` uses `shared_limit` so guessing tokens shares one per-IP bucket (verify.py:16-24) and returns only non-sensitive fields of finalized documents; `/metrics` 404s without a token and compares constant-time (main.py:214-232); `/docs`/`/redoc`/`/openapi.json` off in production (main.py:58-68).
- **Row visibility allowlist**: `scope_evaluations_for_role` ends in 403 for unknown roles (no default-open); employee gets an empty page both in scope and after (evaluations.py:647-659, 710-711); HR self-case and shielded cases excluded from list and export (verified: detail of own case 403, shielded detail 403); `_not_found` merges 404/403 to stop id enumeration (evaluations.py:116-130).
- **Both paths for every guard**: copilot evaluation tools delegate to the endpoint functions (`create/advance/comment/invite` call the real endpoints; `search/get` call `scope_evaluations_for_role`/`_ensure_can_view`); `advance_evaluation` mirrors `require_chain_stage`/`require_roles` in `_ADVANCE` and no longer bypasses `finalize_scoring` — `apply_transition` re-checks no-result and no-snapshot finalization for *both* paths (workflow.py:592-601, 608-662); `hr_claim`'s role gate is duplicated exactly because it has no transition (tools/evaluations.py:189-208, 323); tool registration is fail-closed (base.py:177-197) and re-guarded at confirm time with fresh caps (confirmations.py:94-104); write switch enforced at execution (base.py:359-366).
- **Capability split**: `require_capability` never consults role (deps.py:169-193); self-grant blocked, last-holder guard present (administration.py:206-237); copilot `grant_capabilities`/`set_evaluation_access`/`update_user`/`separate_personnel` delegate to the same endpoints/guards (people.py:1171, 1012, 735, 507).
- **Chain/shape pairs**: `hr_panel_is_shielded` ≡ `IS_SHIELDED_FROM_HR_PANEL`; `IS_ON_CEO_DESK` ≡ `ceo_finalize` from+guard; chain.py seats ↔ TRANSITIONS cover all shapes incl. CEO-direct (`hr_finalize_direct_ceo`) and the four HR-subject variants; separation path (`cancel_on_separation`, seat-vacancy notify) intact; open-record uniqueness backstopped by the partial index with 409 handling (evaluations.py:434-496).
- **Production config guards**: demo-seed refusal, wildcard trusted-proxy refusal, CORS/PUBLIC_BASE_URL checks (config.py:233-292); startup demo-credential scan is fail-closed (startup_checks.py:35-58).

## Could not check, and why
- `docs/review-findings.md`, `docs/nafashr-port-candidates.md`, `docs/nafashr-reported-bugs-check.md` — **absent from this checkout** (docs/ contains `open-findings.md`, `open-findings-ai.md`, `perf-3b.md`, `roadmap.md`, …). I read the two open-findings files instead, after my findings were formed; no overlap with items 1-4 (closest neighbours, items 2/3 of open-findings-ai, are the tools-side copies this review found drifted again in context.py).
- Real-model copilot behaviour (tool-choice quality, prompt-injection resistance in live traffic) — no API key in this environment; the offline harness is tested (48 scenarios) but out of scope for authz.
- Multi-worker rate-limit semantics — counters are in-process by design unless `RATE_LIMIT_STORAGE_URI` is set; ALREADY DECIDED, not re-tested.
- Browser e2e (no display) — not needed for this angle; API-level e2e harness not exercised beyond the targeted scripts above.

## Proposals — only in `Mode: defects + elevation`, max 6, ranked
1. **"چرا فلانی وارد نمی‌شود؟" — یک صفحه برای HR.** Today a lockout reaches HR as a one-off notification and the remedy (`POST /api/users/{id}/unlock`) exists, but HR cannot see *currently locked accounts* without spelunking the audit log. User-visible outcome: HR opens one panel and sees who is locked, until when, and unlocks with one audited click. Change: one read endpoint over `login_attempts` (`locked_until > now`) + a small panel section. Files touched: `users.py`, one schema, one frontend section. Breaks nothing (read-only + existing audited unlock).
2. **Let a forced-password-change user see and revoke their own sessions.** The allowlist (deps.py:17-36) blocks `/api/auth/sessions` and `DELETE /api/auth/sessions/{id}`, so someone compelled to change a password on a possibly-compromised machine cannot cut off a stolen device at the same time. Both endpoints are strictly self-scoped already (auth.py:304-347). Change: add the two paths to the allowlist. Files touched: `api/deps.py` (one line) + tests.
3. **One seat-visibility rule, one function.** Findings 1-4 share a root cause: the "seats this user occupies" predicate is rewritten in five places (scope, personnel columns, role-overview, copilot queue, prompt context) and each copy has already drifted once. Change: a single `seats_of_user(db, user_id) -> ColumnElement` in `services/workflow.py` (next to `may_act_at`), consumed by all five; add a parity test mirroring `test_personnel_visibility_scope.py` for evaluations. Files touched: `evaluations.py`, `personnel.py`, `dashboard.py`, `ai/tools/evaluations.py`, `ai/context.py` + one test file. This is drift-proofing; it does not change any current decision except the bug in finding 1.
4. **Say "suppressed" where a number is suppressed.** In the executive/unit views, `suppressed_avg` returns `null` and the row renders like "no data" — an HR user cannot distinguish "too few people to show" from "nothing happened". User-visible outcome: the chart labels small cohorts "میانگین برای کمتر از ۵ نفر نمایش داده نمی‌شود" instead of a blank. Change: return the cohort count alongside each row (already computed) and let the frontend render the label. Files touched: `analytics.py`/`reports.py` serializers + one frontend tooltip. No change to the suppression rule itself, so no privacy effect; nothing touches snapshots or the signed document.

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 02 REVIEW
# ══════════════════════════════════════════════════════════════════

# P02 — Workflow state machine (approval chain) — defect review

Repo: NexaHR @ 27c83e5 (main). Mode: defects. Reviewer agent: Task 4-b.
Executed: full backend suite already green (1542 passed, /tmp/pytest_backend2.log); plus my own scratch-DB executions (DB `nexahr_p02`, created and dropped) of the real `TRANSITIONS` machine, the real `scope_evaluations_for_role` query, and the real `notify_for_workflow_action` over all 8 chain shapes × all 22 transitions.

## Verdict — 3 lines
The core state machine is sound: an exhaustive walk of all 8 chain shapes (sup × deputy × hr_skipped) through the real `TRANSITIONS` table found zero stuck states, zero dead ends, `finalized` reachable in every shape, and the deputy-bypass guard on `ceo_finalize` holding everywhere. HTTP and copilot paths share the same endpoint functions for every workflow action, and all paired invariants I checked (OPEN vs partial index, shield pair, CEO-desk pair, chain.py vs TRANSITIONS) agree. Four defects survive, all in the *periphery* of the machine — one list-scope drift that hides a case from its own scorer (executed and reproduced), and three notification/queue gaps — none of them a guard bypass or an integrity hole, none a regression of the ALREADY FIXED list.

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. Medium — `backend/app/api/routers/evaluations.py:641-646` (`scope_evaluations_for_role`) vs `backend/app/api/routers/evaluation_access.py:43-47`**
- What breaks: the evaluations list binds each chain role to exactly one seat field (`unit_supervisor` → `unit_supervisor_user_id == user.id`, `deputy` → `deputy_user_id == user.id`, `ceo` → `ceo_user_id == user.id`), while seat *creation* deliberately admits higher-rank users into lower seats via `may_act_at`. A user seated in a seat that outranks his role cannot see the open case he is legally required to act on — in `/api/evaluations` (list), `export.xlsx` filters, and the copilot `search_evaluations`, which all reuse this function. The detail-page guard `_ensure_can_view` (evaluations.py:193) uses all three seats (`allowed_ids`), so the two guards disagree: the case is openable by id but invisible in every list/queue.
- How to reach it (executed, VERIFIED): HR sets `unit_supervisor_user_id = <deputy-role user>` for a personnel (allowed: `may_act_at(deputy, unit_supervisor)` is True at evaluation_access.py:43), deputy calls `POST /api/evaluations` (create guard passes: he is the scorer seat), scores it. Running the real `scope_evaluations_for_role(select(EvaluationRecord), CurrentUser(role=deputy, …), db)` against that DB state returns `[]` — his own pending case never appears in his list; dashboard queue, export and copilot search ditto. Same blindness for a CEO seated in the unit_supervisor seat with a different CEO in the ceo seat (a legal, non-redundant shape), waiting at `hr_approved`.
- Fix: for the three chain roles, scope by *seats* like the detail guard already does — `or_(unit_supervisor_user_id == user.id, deputy_user_id == user.id, ceo_user_id == user.id)` — or at minimum add the over-rank seats per role. Seat-based, so it opens nothing to non-seat-holders (same allowlist semantics as `_ensure_can_view`).

**2. Low/Medium — `backend/app/services/notifications.py:389-505` (`notify_for_workflow_action`); transitions at `backend/app/services/workflow.py:427` and `:442`; dispatched from `backend/app/api/routers/evaluations.py:1170` and `:1190`**
- What breaks: the notification switch has no branch for `ceo_return_no_deputy` and `ceo_return_no_deputy_hr_subject` — the only two of the nine return actions (and 22 transitions overall) that produce **zero** notifications in every chain shape. When the CEO returns a no-deputy-chain record (`hr_approved → submitted`), the HR queue is told nothing (compare `deputy_return`/`ceo_return_manager`, which notify `_hr_queue_ids`); in the HR-subject variant (`hr_approved → draft`), the unit supervisor who must re-score is told nothing (compare `ceo_return_ceo_only`/`ceo_return_manager_hr_subject`, which notify `_tell_the_scorer`). The case silently reappears in the HR queue or the scorer's draft; only the `was_returned` flag and, days later, the SLA sweep reveal it.
- How to reach it (executed, VERIFIED): no-deputy chain (supervisor + CEO, no deputy, HR review on): submit → hr-approve → CEO `POST /api/evaluations/{id}/return {"reason": …}` → 200, status `submitted`; calling the real `notify_for_workflow_action(db, record, "ceo_return_no_deputy")` creates 0 notification rows (matrix executed over all 8 shapes; every sibling return action notifies its owner). Same for `ceo_return_no_deputy_hr_subject` (0 rows; correct recipient would be the scorer).
- Fix: add two branches — `ceo_return_no_deputy → _hr_queue_ids(db, record)` and `ceo_return_no_deputy_hr_subject → _tell_the_scorer(record, evaluator_id)` — and add a parity test that locks every key of `TRANSITIONS` to a notification branch, so the next return transition cannot ship silently.

**3. Low — `backend/app/services/ai/tools/evaluations.py:549-551` (`my_open_cases`, CEO branch)**
- What breaks: the copilot's "my open cases" for the CEO tests only `status == deputy_approved`, but the CEO's desk legally also holds `hr_approved` when the deputy seat is empty (no-deputy chain, incl. its HR-subject variant) — exactly what `IS_ON_CEO_DESK` (`workflow.py:166-169`) and the UI queue (`?on_ceo_desk=true`, fixed and tested in `test_no_deputy_chain.py:200`) already include. The CEO who asks the assistant sees no waiting case that the UI shows; the two views disagree, in the direction of "nothing needs me".
- How to reach it: no-deputy chain, record at `hr_approved`; CEO opens the copilot and asks for his open cases → the tool's CEO branch condition excludes it (deterministic condition; VERIFIED by reading — the branch is a two-line WHERE with no other source of rows).
- Fix: build the branch from `workflow.IS_ON_CEO_DESK` (`& (ceo_user_id == ctx.user.id)`) instead of the bare status equality.

**4. Low — `backend/app/api/routers/evaluations.py:1661-1665` (`reassign_stage_owner`) vs `backend/app/api/routers/evaluation_access.py:43-47`**
- What breaks: reassignment demands an *exact* role match (`new_user.role != expected_role → 400`), while seat creation deliberately allows over-rank seats (`may_act_at`). A vacated seat that was legally held by a higher-rank user (CEO in the unit_supervisor seat — the shape `single_decider` documents and the DB explicitly permits; deputy in the supervisor seat likewise) cannot be given back to the same over-rank role: the rescue tool that the "seats vacated" notification explicitly tells HR to use (`notifications.py:310-313`) rejects the natural replacement. HR falls back to an exact-role user, or, in an org with no active unit_supervisor account, to cancellation only.
- How to reach it: create a chain with the CEO as unit_supervisor seat holder (allowed at access creation), the holder leaves the organization (`_close_out_departure` cancels only the departed person's *own* case and notifies HR about their seats in others' cases), HR calls `POST /api/evaluations/{id}/reassign` with `stage_field="unit_supervisor_user_id"` and `new_user_id=<any ceo- or deputy-role user>` → 400 «باید نقش «مسئول واحد» داشته باشد».
- Fix: replace the equality check with `may_act_at(new_user.role, expected_role)` (mirroring access creation), keeping the subject and redundant-pair guards unchanged.

## Verified correct
- **TRANSITIONS completeness, all 8 chain shapes** (sup/dep/hr_skipped cross product), executed against the real table with the real `_transition_block`: every shape reaches `finalized`, no reachable non-terminal status lacks an outgoing action, no shape leaves a status stranded (this is the "chain.py admits it, TRANSITIONS has no path" invariant — it holds, including all four HR-subject shapes and the no-deputy shapes).
- **`ceo_finalize` guard**: from `hr_approved` it is refused whenever a deputy seat exists (executed for every shape) — no deputy-approval bypass; and `IS_ON_CEO_DESK` (`workflow.py:166-169`) is exactly the query image of that from_statuses+guard pair, with the UI queue test locking both.
- **`hr_finalize_direct_ceo`**: CEO-direct records finalize at HR (`hr_finalizes_record` guard), with snapshot stamping via `_stamp_finalization` in `before`, and the same "no result / no snapshot → 400" second guard inside `apply_transition` (workflow.py:608-624, 655-662) covering both finalize paths and both HTTP and copilot callers.
- **Return machinery**: `_return_action_for` derives the action from the seat via `transition_is_available` (same machine as execution); the role table remains only for error messages; every return requires a reason, writes comment + `evaluation_returned` audit event; `deputy_return_hr_subject`/`ceo_return_*` destinations correctly skip dead stages (each verified in the executed matrix).
- **Cancellation**: `cancel` (HR, any open status, `ensure_hr_may_handle` inside `apply_transition`), `cancel_hr_subject` (deputy/CEO, only shielded HR-unit records, `ensure_may_administer` narrows), `cancel_on_separation` (bypasses the shield *only* on the separation path, still refuses self-decision — `personnel.py:613`, `workflow.py:484-492, 598-601`). Shielded open records are invisible to HR in list (`~IS_SHIELDED_FROM_HR_PANEL`), detail, comment and handover paths — the row-check and the query filter are word-for-word the same rule.
- **HR claim/handover**: claimable-if-unassigned claim logic lives once in `_transition_block`/`apply_transition` (implicit claim audit `hr_case_claimed`), explicit claim/handover refuse non-open records, inactive/wrong-role/wrong-person targets, and — for handover — handing the case to its own subject (evaluations.py:1465-1469). Copilot `hr_claim` delegates to the endpoint with an exact-role pre-gate (`_ADVANCE`), closing the old Depends-only gap.
- **Stage reassignment**: refuses non-open records, empty seats, inactive users, the subject as replacement, and redundant pairs after the change (evaluations.py:1667-1684); requires `ensure_may_administer` (so shielded records are adminstered by CEO/chained-deputy only); notifies the new owner. (The one defect above is role-matching strictness, not a hole.)
- **Submission window & extensions**: `ensure_open` guards `submit` (all six submit actions dispatch through it, evaluations.py:962-969) and self-assessment (me.py:229); extensions are date-based, only while `draft`, refused for period-less records, refuse to shorten the window (`window_for` takes the later date), audit + comment + scorer notification — including the CEO-direct scorer (verified: extension notifies `scorer_field`, test_no_deputy_chain.py:230 covers both directions). Copilot `submit` passes through `ep.submit_evaluation`, so the window guard holds on that path too.
- **Copilot parity for every workflow action**: `advance_evaluation` routes each action to the real endpoint function (submit/hr_approve/deputy_approve/ceo_finalize/return/cancel/hr_claim), re-checking the role gate the `Depends` would have run; `create_evaluation` and `add_evaluation_comment` likewise; `invite_self_assessment` passes through the module switch in the endpoint. No copilot tool writes `EvaluationRecord.status` directly.
- **Paired invariants checked for drift**: `OPEN_STATUSES`/`IS_OPEN_RECORD` vs the partial unique index (`NOT IN ('finalized','cancelled')` — equivalent, six statuses total); `hr_panel_is_shielded` vs `IS_SHIELDED_FROM_HR_PANEL` (identical); `IS_ON_CEO_DESK` vs `ceo_finalize` (equivalent); `SEAT_ROLE`/`SEAT_ORDER`/`scorer_field`/`hr_finalizes` in `models/chain.py` vs their consumers (scorer_seat, owner_after_hr_review, document_signatories, SLA sweep `_current_owner_ids`, extension notifier — all route through the shared helpers, no local rewrites found); `_REDUNDANT_PAIRS` and the DB check constraints agree on which equalities are forbidden vs disclosed.
- **Already-fixed items re-checked on this angle, no regressions**: CEO-direct path incl. HR finalization; `hr_review_skipped` + shield + `cancel_on_separation` + `bypasses_hr_unit_shield`; `add_comment` calls `ensure_hr_may_handle`; role-change/deactivation blocked while holding an open seat (`ensure_no_open_chain_seat` wired at `users.py:271`); separation notifies HR of vacated seats incl. the shielded-records split; `single_decider` compares scorer with `final_approver_user_id` (recognises CEO-direct).

## Could not check, and why
- **HTTP-level reproduction of findings 2–4 through the repo's own pytest suite**: no existing test asserts notifications for `ceo_return_no_deputy*` or the copilot CEO-queue condition, and I may not add test files to the repo. I verified them instead by executing the real functions (`notify_for_workflow_action`, transition guards) against a scratch database (`nexahr_p02`, schema via `Base.metadata.create_all`, seeded rows) and by deterministic code reading of the dispatch sites; the scratch DB was dropped afterwards. Finding 1 was verified with the real scope function and real SQL on the same scratch DB.
- **Frontend rendering of queues/labels** (e.g., whether any UI surface compensates for the finding-1 list gap by another query): frontend review belongs to other agents' prompts; the frontend lint/test jobs were running in the background, and this review's scope is the backend state machine.
- **`services/audit_events.EVENT_LABELS` three-way lock re-derived by hand**: the suite (incl. `tests/test_audit_event_labels.py`) is green on the current HEAD, which is the enforcement mechanism the prompt describes; I did not independently re-enumerate all 81 labels.
- **Launcher tests and the e2e mock-LLM scenario**: owned by the parallel agents (logs `/tmp/launcher.log`, `/tmp/fe_lint_test.log`); not re-run here to avoid duplicating a 2-core machine's work.

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 03 REVIEW
# ══════════════════════════════════════════════════════════════════

# P03 — Scoring & legal PDF (compute_result, per-indicator weights, absent-section redistribution, scheme versioning & mid-cycle activation, bonus rules, snapshot.py, documents.py, pdf.py, hash + QR verify)

## Verdict — 3 lines
The score engine and the signed-document pipeline are sound where it is easy to be wrong: redistribution, weighted schemes, threshold bands, bonus clamping, scheme stamping/mid-cycle activation, copilot delegation, signatories, and the hash→QR→verify chain were all exercised live on a scratch DB and behave correctly, with no regression of any ALREADY FIXED item. Three LOW defects remain, all of the "the printed page is not quite what the screen said" class: the legal PDF's row order is unspecified (plan-dependent — demonstrated live: the same finalized data yields two different document orders under two query plans), the frontend bonus preview can disagree with the document by 0.01 on a 3-decimal input, and the read API still shows only the raw bonus while the document prints the clamped one (the repo's own open item, re-verified reproducing today).

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. LOW | `backend/app/services/snapshot.py:63-68` and `:71-75` (consumers `:146-156`, `:157-164`; template `backend/app/templates/evaluation_summary.html:143`, `:162`, `:226`) | The row order of the legal PDF is unspecified — none of the three snapshot selects has an `ORDER BY`, so the order frozen into the hashed document is whatever query plan the finalize connection happens to get.** `build_final_snapshot` loads `EvaluationScore` and `EvaluationComment` with bare `select(...).where(...)` and no ordering; the template then prints `snapshot.scores` into the «شاخص‌های عمومی / تخصصی» tables and `snapshot.comments` into «کامنت‌های مراحل بررسی» in exactly that order. *Executed, not reasoned:* on a scratch DB I finalized a 20-indicator record whose scores were PUT in reversed display order, then called `build_final_snapshot` twice on the identical finalized row — under the default plan the scores come out `[1,2,3,…,20]` (a plan using `uq_evaluation_scores_record_indicator` returns indicator order), with `enable_indexscan/bitmapscan/indexonlyscan=off` (seq scan) they come out `[1,13,20,19,18,…]` — i.e. exactly the raw payload order of the PUT, confirmed as the physical heap order via `ctid` (`(0,1)→ind 1, (0,2)→ind 13, (0,3)→ind 20…`). Worse, every draft auto-save PUT rewrites *every* scored row (`_replace_scores` upserts the full payload with `on_conflict_do_update`, `evaluations.py:345-355`), so heap order equals the *last save's* payload order — any API client controls it, and even the UI's own edit sequence reshuffles it. Whichever order the finalize-time connection's plan produces is frozen into `final_snapshot` and printed on the signed document; two records with identical inputs can print in different orders, and a record can print its indicator rows in an order that matches neither the form (`section, display_order` — `indicators.py:74`) nor the id order. How to reach: any scorer (or direct API integration, e.g. the copilot's `create_evaluation` + a custom client) PUTs `/scores` with rows in a non-display order or edits a score via auto-save → chain finalizes under a heap-order plan → the PDF's evidence tables are permuted relative to the form the evaluator saw. Comment order is the same defect (`:66-68`) but in practice both natural plans return heap = chronological order here (verified live), so the scores tables are the exposed half. No number changes — but this is the signed document's evidence column, and its order is currently an accident of query planning. Fix: in `build_final_snapshot`, order scores by the record's own framework indicator order — join `Indicator` and `order_by(Indicator.section, Indicator.display_order, Indicator.id)`, the same source the form uses — and comments by `(EvaluationComment.created_at, EvaluationComment.id)`; note the codebase already treats this order as intended (`EvaluationComment.comments` relationship declares `order_by="EvaluationComment.created_at"` at `models/evaluation.py:210-214`, and `_persisted_scores` orders by id at `evaluations.py:298-302`) — the snapshot's raw selects are the only readers that bypass it. New snapshots only; archived PDFs stay byte-frozen by design.

**2. LOW | `frontend/src/components/ScoreForm.tsx:213` (render `frontend/src/pages/EvaluationDetailPage.tsx:922-925` and `:1133-1139`; backend twins `backend/app/services/evaluation.py:241` + `Numeric(4,2)` at `backend/app/models/evaluation.py:133`, accepted raw at `backend/app/schemas/evaluation.py:235`) | The «+ Y امتیاز ویژه» preview can differ from the signed document by 0.01 — the `appliedBonus` twin still rounds half-up on the double while the server pipeline rounds the stored decimal half-away-from-zero.** `appliedBonus` computes `Math.round(capped * 100) / 100` on the *typed* value; the server stores the typed value into `bonus_points Numeric(4,2)` — PostgreSQL rounds it half-away-from-zero — and `applied_bonus`/the document then use the stored value. Concrete path (both halves executed): evaluator types bonus `1.005` (schema accepts any float ≥0; PATCH returned 200 and read back `1.01`) → the form's preview computes `Math.round(100.49999999999999)/100 = 1.00` and shows «+۱» beside the ring (the ring's 1-decimal total still agrees), while the finalized document prints «امتیاز ویژه +۱٫۰۱». This is the remaining sibling of the `round1` fix the repo already shipped (`utils/rounding.ts` implements Python-compatible decimal expansion for the *base* percentage; `appliedBonus` was missed). Two corrections to the naive framing of this bug: (a) the `2.675` example *agrees* — `2.675*100 === 267.5` exactly in IEEE, so both sides give 2.68; divergence needs a value whose double product lands below the tie (e.g. `1.005`, `0.145`→`0.14` vs PG `0.15`); (b) Python's banker's `round(...,2)` at `evaluation.py:241` is not the diverging step — at finalize it only ever receives the already-2-decimal DB value. Reach: any evaluator who types a 3-decimal bonus (free-text input, no step constraint client- or server-side) with base ≤ 97-ish so the clamp doesn't already cut the third decimal. Fix: quantize the raw to 2 decimals in one place — either a `round2` built like `round1` (decimal expansion, tie-to-even won't match PG's half-up, so prefer…) — or, simpler and airtight, round the typed bonus to 2 decimals *in the form before preview and submit* (`Math.round(v*100)/100` on the *string*-parsed decimal, or a step=0.01 input) and reject >2-decimal values server-side in `SpecialScoreUpdate`, so preview, DB, and document always compute on the same number.

**3. LOW | `backend/app/schemas/evaluation.py:154` (consumer `frontend/src/pages/EvaluationDetailPage.tsx:769`) | The read API exposes only the *raw* bonus, so the subject's file page shows a number the signed document does not.** `EvaluationRead.bonus_points` is the raw column value (deliberately raw in the DB); the applied value exists only inside the snapshot (`snapshot.py:140` via `applied_bonus_for_document`) and the Excel (`evaluations.py:804-808`). *Executed live:* a record with base 98.0 and raw bonus 5 finalized with applied 2.0 and final 100.0 — `GET /api/evaluations/{id}` reports `bonus_points: 5.0` while the rendered document prints «امتیاز فرم ۹۸٪ + امتیاز ویژه +۲ = امتیاز نهایی ۱۰۰٪». The file page (`ReadOnlyScoring`) shows «امتیاز ویژه: ۵ امتیاز» for the paper that says +۲. Aggravating edge: when the clamp lands exactly on 0 (base 100), `applied_bonus_for_document` returns `None`, the template's `{% if snapshot.bonus_points %}` omits the whole block *including the bonus reason* (`evaluation_summary.html:107-118`), while the file page still displays the raw 5 and its reason — the document shows no trace of an award the organization formally recorded (the audit trail keeps it). This is a fresh-evidence confirmation of the repo's own open item (`docs/open-findings.md`, «سندِ رسمی و محاسبه» → «خطِ "+ ۵ امتیاز ویژه" در صفحهٔ پرونده clamp نمی‌شود»); it is not on the prompt's ALREADY FIXED list — the fixed piece was the *form-side* line beside the ring (CHANGELOG «۹۸ + ۵ = ۱۰۰»), the read-side display is the part still open — so it is not a regression. Reach: any record where `100 − base_pct < raw ≤ bonus_max_points` (perfect scores plus any bonus, or a high base under a small cap) → open the finalized detail page. Fix: add the applied value to the read schema (`applied_bonus_for_document(db, record)` already computes it, is used by the Excel path, and returns `None` when nothing was applied) and show it next to the raw in `ReadOnlyScoring`, the way the document and Excel already do.

## Verified correct
- **`compute_result` math, including absent-section redistribution** — executed directly: only-general and only-specialized frameworks both yield `base=100` for all-5 scoring; weighted scheme (indicator weights {1:3, 2:1}) gives exactly 84.0; the zero-weight-all-sections fallback (simple average) gives 100. Frontend `computePreview` (`ScoreForm.tsx:155-198`) mirrors it step for step — same weight map with string keys matching `AppConfig.indicator_weights`, same present-section renormalization, same fallback — and that fallback is unreachable through validated schemes anyway (`SchemeInput` forces section weights to sum to exactly 1 and indicator weights > 0; re-validated again at activation by `ensure_scheme_is_valid`).
- **Threshold table** — half-open `[lower, upper)` bands leave no gap and no double-label: executed labels at 0/59.99/60/74.99/75/89.99/90/99.99/100; a final of 100 gets the top band; `final_weighted_pct ≤ 100` structurally because the bonus cap sits on the addend (`100 − base`), verified by the clamp execution below.
- **Bonus clamping end-to-end** — executed: base 98 + raw 5 → applied 2.0; base 100 + raw 5 → 0.0; negative → 0.0; raw 7 → cap 5.0. The identity «امتیاز فرم + امتیاز ویژه = امتیاز نهایی» holds on the rendered document (98.0 + 2.0 = 100.0 verified in the generated HTML); the raw stays in `record.bonus_points` and in the `special_score_set` audit event; the applied value is what the snapshot (`snapshot.py:140`), the Excel (`excel.py:178-197` via the router's `applied_bonus_for_document` map), and the PDF print. Write-time `validate_bonus` independently rejects raw > scheme cap, negatives, and reason-less bonuses, backed by two DB CheckConstraints.
- **Scheme versioning and mid-cycle activation** — records stamp both `scoring_scheme_id` and `indicator_framework_id` at creation; `rules_for_record` reads the stamp (never `active_scheme`) and `finalize_scoring` uses it; schemes are immutable once active (no edit endpoint exists; `DELETE` refuses non-drafts); `activate()` enforces draft-only + creator≠activator + `ensure_scheme_is_valid` *inside the service*, so the copilot's `activate_scoring_scheme` (which calls the service directly) cannot bypass them; the detail payload carries the record's own stamped rules (`_to_detail` → `scoring_rules`) and the frontend prefers them over today's active config, so an open record is previewed and validated under its own version even after a mid-cycle activation.
- **Finalize guards on both paths** — the copilot's `advance_evaluation` delegates every action to the real endpoint functions with real payloads; `submit` runs `finalize_scoring`, and `hr_approve`/`ceo_finalize` pass a background-task queue that the tool drains so the PDF archives. There is deliberately no copilot tool for `upsert_scores`/`set_special_score` (verified against the full tool registry). `apply_transition` independently refuses a finalized transition without a result or snapshot, so a skipped `before` cannot close a record without a document.
- **Snapshot versioning** — v6 writer (`SNAPSHOT_VERSION = 6`); every reader tolerates older shapes by key presence, not version dispatch: the template's `{% if snapshot.signatories %}` else-branch rebuilds the ≤v4 block, `self_assessment` (v4+) and `bonus_points` (v2+) blocks are silently omitted when absent, `verify.py` reads only `personnel`. Archived v4 documents render as originally printed. (The module docstring's "render branches on version" is implemented as key-presence branching — the guarantee holds by a different mechanism than the comment implies.)
- **Signatories** — executed for the full chain: «مسئول واحد، منابع انسانی، معاونت، مدیرعامل» from the record's real seats, one row each, `single_decider` False; code-read plus the dedicated suites (`test_document_signatures.py`, `test_direct_ceo_hr_finalization.py`, `test_redundant_chain_seat.py`) cover the merged CEO-in-supervisor-seat row, the no-deputy shapes, and HR omission when `hr_review_skipped`.
- **Hash + QR verify path, exercised live** — `sha256(pdf_bytes) == document.sha256`; `GET /api/verify/{token}` returns 200 with the matching hash, final pct, code and recommendation; `GET /api/verify/{evaluation_code}` (sequential, guessable) returns 404; the token is `secrets.token_urlsafe(24)` stamped only at `ceo_finalize` (`evaluations.py:1057`) with a unique index; the rate limit uses one shared `30/minute` scope so per-token buckets cannot defeat guess-limiting; QR embeds `{public_base_url}/verify/{token}` matching the frontend route.
- **Document archival robustness** — archival is idempotent with an `IntegrityError` race handled inside a SAVEPOINT; render failures never fail finalization; the scheduler sweep backfills finalized-with-snapshot records lacking documents and deliberately skips snapshot-less ones; `/summary.pdf` distinguishes not-finalized (400) from WeasyPrint-missing (explicit 500).
- **PDF hardening & fonts** — the templates-only URL fetcher blocks external and `file://` sources, Jinja autoescape is on (evaluator text cannot inject markup into the signed document), all four woff2 files including `Vazirmatn Latin` exist under `app/templates/fonts/`, and Jalali dates render through `clock.to_local` (Asia/Tehran), not the UTC wall clock.
- **Regression sweep of the ALREADY FIXED items in this angle** — applied (not raw) bonus in document/Excel (verified live), signatories from real seats (verified live), `single_decider` recognises CEO-direct (test + code read), Vazirmatn Latin fonts (files + declarations verified), `core/clock.py` day boundaries (code read: `to_jalali` → `to_local`), `add_comment` calling `ensure_hr_may_handle` and the stage/owner table (code read; the stage guard also fired live — a draft-stage comment attempt got a 403 with the correct Persian message). None is broken.

## Could not check, and why
- **The full backend suite** — already executed by the orchestrator against the shared DB (1542 passed, `/tmp/pytest_backend2.log`); rerunning was explicitly out of scope. My live probes (compute functions in a REPL; a full HTTP finalize→snapshot→PDF→verify run with a bonus-cap scenario, a reversed-payload ordering case, and a plan-dependence case) ran on a private scratch DB (`nexahr_p03`, migrated to head, dropped afterwards) to avoid touching the shared one. Launcher (89 passed) and frontend logs confirmed green.
- **Actual glyph rasterization and QR decoding** — I verified the rendered HTML text, the font files and declarations, and exercised the token endpoint directly; I did not rasterize WeasyPrint output and scan a QR image with a decoder. Font embedding is covered by `tests/test_document_fonts.py` in the green suite.
- **Which plan production will pick, and when** — for finding 1 I demonstrated that two plans produce two different document orders for identical data, and that heap order follows the last PUT's payload order; I could not (and did not try to) characterize the planner's choice across table growth, bloat, and version upgrades. The defect statement rests on PostgreSQL's contract that unordered SELECT order is unspecified, not on a predicted production timeline.
- **A forced observable corruption for finding 2's preview** — the two halves were executed independently (JS `Math.round` on the double in node; PG `Numeric(4,2)` storage + read-back through the real PATCH endpoint), but not inside one browser session against a running frontend; the composed divergence (preview 1.00 vs document 1.01) follows arithmetically from the two verified halves.

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 04 REVIEW
# ══════════════════════════════════════════════════════════════════

# P04 — AI copilot review (backend/app/services/ai/, api/routers/ai.py, frontend/src/components/copilot/)

Repo: /home/z/my-project/NexaHR @ 27c83e5 (branch main). All evidence below was executed against a private scratch DB (`nexahr_p04`, created and dropped) plus one live run of `e2e/run_e2e.sh --api-only`'s scenario (driven manually with the backend venv because the stock script calls bare `python3` without deps; see "Could not check"). Full backend suite: 1542 passed (pre-existing log /tmp/pytest_backend2.log, not rerun).

## Verdict — 5 lines, and say plainly whether the defect half or the elevation half is where the value is
The defect half comes back remarkably clean: 47/47 tools enforce (or deliberately re-validate) the same guards as the UI, the confirmation flow survived every attack I executed (cross-user, double-confirm, expiry, stale world, revoked permission), and a hijacked-model simulation could not turn injection into action beyond a well-labelled card. The five findings are all Low/Info — audit-trail gaps around the confirmation point, one under-inclusive prompt-scope drift, and a defeatable injection frame — none is a privilege boundary that actually breaks. The elevation half is where the value is, but not in the direction the prompt suspects: 7 of the 8 suspected-thin areas were already built in versions 1.12.0–1.14.0 (usage ledger, stale-case digest, rolling summary, diff cards, offline eval harness, rules_text, Persian style enforcement), so the real remaining gap is the one the repo itself records as unmeasured — real-model tool selection (`--live` harness) — plus small accountability polish around the confirmation point.

## Tool parity table — all 43 tools: UI guard | copilot guard | same?
The prompt says 43; **47 tools actually register today** (verified by importing `REGISTRY`; `prompt.py:151` also says ۴۷). Full list below, with the UI endpoint and its `Depends` guard vs. what the copilot path enforces. Note: `ensure_module_enabled` is an in-body call by design, so every delegating tool passes through it too (authorization.py:208-241) — treated as "same" here, not a finding.

| # | Tool | UI path + guard | Copilot guard (registry + body) | Same? |
|---|------|------------------|--------------------------------|-------|
| 1 | search_personnel | GET /api/personnel — `get_current_user` + in-body `sees_all_personnel` scope (personnel.py:214-245) | `guarded_inline` → `_visible_personnel_ids` (people.py:190-203); adds own personnel_id the UI list lacks (people.py:80-82) | YES* |
| 2 | get_personnel | GET /personnel/{id} — `get_current_user` + `_can_view_personnel` (personnel.py:543-556) | `_ensure_can_view_personnel` → same function + explicit self branch (people.py:92-114) | YES* |
| 3 | create_personnel | POST /personnel — `require_role_or_capability(hr, manage_personnel)` (personnel.py:367-371) | caps `manage_personnel` OR role `hr` (people.py:277-279); unique-code + date checks in body | YES |
| 4 | update_personnel | PATCH /personnel/{id} — same guard (personnel.py:642-646) | same declaration + delegates to endpoint function (people.py:384-415) | YES |
| 5 | separate_personnel | PATCH flow → `_close_out_departure` behind same guard | same declaration + delegates to `_close_out_departure` (people.py:484-507) | YES |
| 6 | search_users | GET /api/users — `require_capability(manage_users)` (users.py:110-114) | caps `manage_users` (people.py:539-544) | YES |
| 7 | create_user | POST /api/users — `require_capability(manage_users)` (users.py:165-169) | same cap + delegates to endpoint (people.py:613-653) | YES |
| 8 | update_user | PATCH /api/users/{id} — same guard (users.py:230-234) | same cap + delegates (people.py:691-742) | YES |
| 9 | list_org_units | GET /personnel/org-units — `require_role_or_capability(hr, manage_personnel)` (personnel.py:267-272) | caps `manage_personnel` OR role `hr` (people.py:821-833) | YES |
| 10 | create_org_unit | POST /api/org-units — module `_guard` = same OR-guard (org_units.py:26) | same + delegates (people.py:884-895) | YES |
| 11 | get_evaluation_access | GET /api/evaluation-access — `require_role_or_capability(hr, manage_personnel)` (evaluation_access.py:50-55); the same chain data is also in GET /personnel/{id} payload under `_can_view_personnel` | `_ensure_can_view_personnel` (people.py:920-928) — wider than the *dedicated* endpoint, equal to the personnel-detail disclosure | YES* |
| 12 | set_evaluation_access | PUT /api/evaluation-access — same OR-guard (evaluation_access.py:63-68) | same + delegates incl. all conflict rules (people.py:973-1021) | YES |
| 13 | list_user_capabilities | GET /api/administration/capabilities — `require_capability(manage_capabilities)` (administration.py:129-133) | same cap (people.py:1101-1107) | YES |
| 14 | grant_capabilities | PUT /capabilities/{user_id} — same cap (administration.py:162-166) + last-holder & employee guards in body | same cap + delegates (people.py:1142-1176); card preview diffs named caps (people.py:1193-1234) | YES |
| 15 | search_evaluations | GET /api/evaluations — `get_current_user` + in-body `scope_evaluations_for_role` (evaluations.py:669-676, 613-659) | `guarded_inline` → same function (tools/evaluations.py:85-101) | YES |
| 16 | get_evaluation | GET /evaluations/{id} — `get_current_user` + `_ensure_can_view` (evaluations.py:821-828) | same function (tools/evaluations.py:140-147) | YES |
| 17 | create_evaluation | POST /api/evaluations — `Depends(require_chain_stage(unit_supervisor))` **plus** in-body `may_act_at`+scorer-seat check (evaluations.py:365-430) | `guarded_inline`; Depends is skipped on the function call but the in-body check runs (tools/evaluations.py:169-177) | YES |
| 18 | advance_evaluation | per-action `require_roles`/`require_chain_stage` + `ensure_transition_allowed` + `ensure_hr_may_handle` (evaluations.py:940-1426) | `_ADVANCE` mirror table `_stage`/`_exact` (tools/evaluations.py:204-324) + full endpoint delegation; covered by `test_ai_workflow_parity` | YES |
| 19 | add_evaluation_comment | POST /comments — `get_current_user` + in-body stage/seat guard (evaluations.py:1800-1801) | delegates to `add_comment` (tools/evaluations.py:443-463) | YES |
| 20 | invite_self_assessment | POST /invite-self-assessment — OR-guard + `ensure_module_enabled("self_assessment")` in body (personnel.py:278-284) | same declaration + delegates (tools/evaluations.py:470-486) | YES |
| 21 | my_open_cases | UI queues built on `scope_evaluations_for_role` / `IS_ON_CEO_DESK` (evaluations.py:625-655, workflow.py:166-169) | role branches hand-written (tools/evaluations.py:521-563); HR branch misses own-case exclusion, CEO branch misses hr_approved no-deputy cases | **NO** (finding 5) |
| 22 | explain_evaluation_rules | all five scheme-read endpoints behind `require_capability(manage_scoring)` (scoring_schemes.py:92-96) | `guarded_inline`; opens thresholds/section weights to every role by documented design, keeps indicator weights/evidence rules behind `manage_scoring` (tools/evaluations.py:601-664) | **NO** (deliberate, one-way, read-only) |
| 23 | list_indicators | GET /api/indicators — `get_current_user` (indicators.py:56-60) | `guarded_inline`, no body guard (framework.py:20-33) | YES |
| 24 | create_indicator | POST /api/indicators — `require_capability(manage_scoring)` (indicators.py:84-88) | same cap + delegates incl. `_publish` (framework.py:81-110) | YES |
| 25 | update_indicator | PATCH /{id} — same cap + meaning-change 409 in body (indicators.py:208-212) | same + delegates; `wording_fix_reason` plumbed (framework.py:154-198) | YES |
| 26 | list_scoring_schemes | GET /api/scoring-schemes — `require_capability(manage_scoring)` (scoring_schemes.py:92-96) | same cap (framework.py:208-214) | YES |
| 27 | create_scoring_scheme_draft | POST — same cap; `SchemeInput` validation | same cap; builds `SchemeInput` then delegates (framework.py:307-336) | YES |
| 28 | activate_scoring_scheme | POST /{id}/activate — same cap (scoring_schemes.py:216-220); two-person rule in service | calls service `activate()` directly → same two-person rule + extra audit event (framework.py:356-372) | YES |
| 29 | list_periods | GET /api/periods — `require_roles(hr)` (periods.py:47-51) | roles `(hr,)` (framework.py:382-388) | YES |
| 30 | create_period | POST — `require_roles(hr)` + module switch in body (periods.py:57-62) | same + delegates (framework.py:424-432) | YES |
| 31 | period_progress | GET /{id}/progress — `require_roles(hr)` (periods.py:295-300) | same + delegates (framework.py:447-453) | YES |
| 32 | preview_bulk_evaluations | POST /bulk-create/preview — `require_roles(hr)` (periods.py:435-440) | same + delegates (framework.py:495-507) | YES |
| 33 | run_bulk_evaluations | POST /bulk-create — `require_roles(hr)` + module switch (periods.py:452-458) | same + delegates (framework.py:550-562) | YES |
| 34 | search_improvement_plans | GET /api/improvement-plans — `require_plan_role` + owner filter for non-HR (improvement_plans.py:192-207) | same roles + same owner filter (framework.py:584-607) | YES |
| 35 | create_improvement_plan | POST — `require_roles(hr)` (improvement_plans.py:222-226) | same + delegates (framework.py:649-672) | YES |
| 36 | update_improvement_plan_goal | PATCH goal — `require_plan_role` + `_ensure_plan_open` + owner (improvement_plans.py:511-516) | same roles + delegates (framework.py:692-712) | YES |
| 37 | report_summary | GET /api/reports/summary — `require_roles(hr)` (reports.py:218-224) | roles `(hr,)` + `_hr_only` in body + delegates to `_summary_data` (analytics.py:30-76) | YES |
| 38 | employee_vs_unit | GET /employee-vs-unit — `require_roles(hr)` (reports.py:285-291) | same + delegates (analytics.py:79-111) | YES |
| 39 | dashboard_overview | GET /api/dashboard/overview — `require_roles(hr)` (dashboard.py:159-163) | same + delegates (analytics.py:114-134) | YES |
| 40 | expiring_contracts | GET /expiring-contracts — `require_roles(hr)` (dashboard.py:476-481) | same + delegates (analytics.py:137-168) | YES |
| 41 | executive_analysis | GET /api/analytics/executive — `require_roles(ceo, deputy)` (analytics router 243-247) | roles `(ceo, deputy)` + delegates (analytics.py:171-184) | YES |
| 42 | my_scoring_analysis | GET /my-scoring — `require_roles(unit_supervisor, deputy)` (analytics router 66-72) | same roles + delegates (analytics.py:187-200) | YES |
| 43 | search_audit_log | GET /api/audit-log — `audit_log_reader` = `view_audit_log` OR `view_diagnostics` (deps.py:234-260) + in-body scope | same OR caps + same scope (`SYSTEM_EVENT_TYPES`, record-null belt) (analytics.py:203-280) | YES |
| 44 | my_permissions | GET /api/administration/my-permissions — `get_current_user` (administration.py:60-66) | `guarded_inline`, self-only payload (analytics.py:306-327) | YES |
| 45 | inspect_upload | POST /api/ai/conversations/{id}/attachments — `get_current_user` + `config.allow_uploads`, own conversation only (ai.py:387-401); staging never becomes personnel_import for non-importers (uploads.py:153-171) | `guarded_inline` + `_upload_or_404` own-user/own-conversation (uploads.py:270-276) | YES |
| 46 | patch_upload_rows | UI import-fix path behind `hr OR manage_personnel` (personnel.py:496-501) | caps `manage_personnel` OR role `hr` + committed guard + column verification (uploads.py:325-389) | YES |
| 47 | import_personnel | POST /api/personnel/import — `require_role_or_capability(hr, manage_personnel)` (personnel.py:510-515) | same declaration + executor re-validates from raw bytes at confirm (uploads.py:430-455, 465-486) | YES |

`YES*` = same or stricter on the row-level rule that actually governs the data; the copilot adds only the caller's own row / equals the wider of two UI disclosures. Both `same? = NO` rows are intentional-or-cosmetic: #21 is the two queue-branch drifts of finding 5, #22 is a documented transparency decision (tools/evaluations.py:602-621) that only *widens* a read-only view and hides the gameable internals from non-`manage_scoring` users.

## Findings — severity | file:line | what breaks | how to reach it | fix

| Severity | file:line | What breaks | Concrete path (role, inputs, state → wrong result) | Fix |
|---|---|---|---|---|
| Low | app/services/ai/confirmations.py:104-108 | A confirm-time refusal (`tools_base.guard` 403 / `allow_writes` 403) is raised **before** `_claim_for_execution` and is never logged: the row stays `pending`, the card stays in `list_pending` and the conversation view until the 24 h TTL, and **no audit event is written** — executed: revoked the proposer's `manage_capabilities` between proposal and confirm → 403, row #6 remained `pending`, zero events in `audit_log` for it (while proposal-time refusals do get `ai_tool_refused`, orchestrator.py:197-215). | HR proposes `grant_capabilities` (card drawn) → admin revokes the holder's `manage_capabilities` → HR clicks confirm → 403 "اجازهٔ تغییر داده ندارید"; card persists; repeated clicks leave no trace. | Log an event on this path (`ai_tool_refused`, reason `confirm_blocked`) and mark the row `failed`/`expired` instead of leaving it `pending`. |
| Low | app/services/ai/orchestrator.py:247-263 | A risky proposal itself produces **no audit event**: `AiPendingAction` is created without `log_event` (contrast read-only tools → `ai_tool_invoked` at tools/base.py:409-420). Who *confirmed* is reconstructable (`ai_action_confirmed`, confirmations.py:186-197), but "the model proposed X and no human ever decided" is only reconstructable from chat `meta_json` and dies at the 30-day purge (`purge_decided_actions`, confirmations.py:252-272, wired at services/scheduled.py:571). | Any risky tool → card created → user ignores it → 30 days later the only trace of the proposal is gone; auditor sees the conversation text but no queryable event. | Emit `ai_action_proposed` (sanitized arguments, pending_action_id) at creation. |
| Low | app/services/ai/context.py:42-67 | `_visible_personnel_ids` lacks the `EvaluationRecord`-seat union its twin in tools/people.py:57-79 got in 1.11.1. Under-inclusive, not a leak — but it silently breaks the file's own rule 1 ("the prompt must not see a different world than the tools"): a person who is in `search_personnel` scope is absent from the prompt scope. Reproduced: supervisor sits on `EvaluationRecord.unit_supervisor_user_id`, the `EvaluationAccess` row points elsewhere → `search_personnel` scope includes the subject, `context` scope does not. The parity test only covers the tools copy. | Supervisor scores a case → HR later repoints the chain's access row → supervisor asks the copilot "who is X?": `search_personnel` returns X, the context block never mentions X. | Extract one shared scope helper (or add the same union) and point the parity test at `context.py` too. (= P01 finding #4; independently reproduced here.) |
| Low | app/services/ai/prompt.py:85, 88-89 (+ orchestrator.py:550) | The injection frame is defeatable by data: `_FENCE = "─" * 40` is a data-controlled delimiter — a personnel named `"─"*40 + "…"` closes the frame early and the attacker's text sits *outside* the data region (executed: forged-frame text lands in a segment between fences). And the framing is asymmetric: system-prompt data is framed, but **tool results** (`role="tool"`, orchestrator.py:550 — personnel names, evaluator comments/objection text, every inspected spreadsheet cell via `inspect_upload`/`enrich_excel_structure`) reach the model raw. Verified limits of the blast radius: a simulated hijacked model calling `grant_capabilities` was refused 403 for a plain HR; with a `manage_capabilities` holder the attack produces exactly one pending card whose preview names the real diff in Persian labels, and confirm re-validates (employee target refused 400 by the endpoint). So compliance does not *succeed* silently — the residual risk is a privileged user rubber-stamping a card whose *context* was manipulated. | HR imports/edits a workbook or reads a case whose free-text field carries the forged fence or an instruction; the model obeys; everything downstream is guarded but the human decides on attacker-shaped context. | Escape/strip runs of U+2500 ≥ 8 in `_untrusted` bodies, and state in `_PROTOCOL_RULES` that tool results are data too (one line), or wrap tool content with the same frame. |
| Low | app/services/ai/tools/evaluations.py:530-532, 549-551 | `my_open_cases` queues drift from the UI queues: HR branch takes `status == submitted` with no own-case exclusion, while the UI list excludes the HR member's own record (evaluations.py:637-640); CEO branch tests only `deputy_approved`, while `IS_ON_CEO_DESK` also includes `hr_approved` with no deputy (workflow.py:166-169). | HR member's own case sits on their copilot queue (UI hides it); a CEO of a no-deputy chain never sees hr_approved cases on the copilot queue although the UI shows them. | Build both branches from `scope_evaluations_for_role`/`IS_ON_CEO_DESK` constants instead of hand-written conditions. (= P01 #3 and P02 #3; conditions re-verified here.) |
| Info | app/services/ai/orchestrator.py:253-258 + confirmations.py:110 | The confirmation card's before-state is a proposal-time snapshot by design (preview_json); the **arguments** cannot drift (single writer — `AiPendingAction(` is constructed only at orchestrator.py:247; confirm re-reads `arguments_json` at confirmations.py:110), and confirm re-validates permissions/state (all attacks executed: cross-user → 404, double-confirm → 409, expired → 410, finalized-under-cancel → 400 + row `failed`). But the *diff rows* can misdescribe today's world (e.g. `grant_capabilities` "removed" rows computed against caps that have since changed). | Propose `grant_capabilities` → someone changes the target's caps → confirm: the executed target set is exactly right, but the card's removed/added rows can disagree with the real before→after. | Recompute the preview at confirm render and show "وضعیت امروز" beside the snapshot, or flag the card when a world-hash differs. |

**REGRESSION check against the ALREADY-FIXED list — none found:** `get_evaluation_access` now runs `_ensure_can_view_personnel` (people.py:920-928); `separate_personnel` delegates to `_close_out_departure` so `revoke_all_for_user(db, …)` is called correctly (people.py:484-507); scheme draft builds `SchemeInput` and delegates (framework.py:307-336); a tool declaring neither capabilities nor roles nor `guarded_inline` raises at import (tools/base.py:180-184); `/api/ai/chat` carries `@limiter.limit(settings.ai_chat_rate_limit)` (ai.py:471-472); personnel scope is the single `sees_all_personnel` (authorization.py:23-42) with no stale copy.

## Verified correct — name what you checked and found sound
- **Guard registration is fail-closed and current.** 47 tools import cleanly; `is_allowed` OR-semantics match the OR-guards of the routers; the deliberate `guarded_inline` risky set is exactly `[add_evaluation_comment, advance_evaluation, create_evaluation]` (enforced by test_copilot_eval.py:324-340) and all three re-validate in-body on the copilot path (create_evaluation's in-body `may_act_at`+scorer check at evaluations.py:426-430 is the load-bearing one, since `Depends` does not run on direct function calls).
- **Delegation, not duplication.** Every write tool hands a real Pydantic payload to the endpoint function (people.py, framework.py, tools/evaluations.py, uploads.py), so module switches, wording-fix 409s, scheme validation, import re-validation, two-person activation, and `ensure_hr_may_handle` all execute on the copilot path. `ensure_module_enabled` being an in-body call passes through by construction.
- **The confirmation flow under attack — all executed, all held:** cross-user confirm → 404 (`_pending_or_404` user_id + conversation owner); double-confirm → 409 via the atomic conditional `UPDATE … WHERE status='pending'` claim (confirmations.py:39-61); expired → 410 with the row flipped to `expired`; stale world (record finalized under a pending `cancel`) → 400 from the state machine and the row marked `failed`; permission revoked mid-flight → 403 before claim; TTL 24 h (`uploads.py:36`, models/ai.py:282-287) and the 30-day purge wired into the scheduled sweep (scheduled.py:571).
- **Injection cannot become silent execution.** Live hijacked-model run: plain HR → guard 403; `manage_capabilities` holder → exactly one card, whose preview names the account and the added/removed capabilities in Persian (people.py:1193-1234), and confirm executed precisely the card's diff; the employee-target case was refused 400 by the endpoint's own guard. Secrets never reach cards (`_SECRET_ARGUMENTS`, tools/base.py:487) or logs (`_SENSITIVE_KEYS` recursive sanitizer, tools/base.py:325-349).
- **Context is an access-control surface and holds.** Executed per-role builds: employee (own row only, no accounts/units/evaluations), unit supervisor with two reports (his two reports, not the next unit's, no accounts), support (no personnel/units/accounts/evaluations blocks at all), HR (map + accounts + people). Cross-checked `context._visible_personnel_ids ⊇ {rows the same user can read via GET /personnel/{id}}` for all three target roles — no over-inclusion. Accounts block gated on `manage_users`, units block on the same rule as `/personnel/org-units`, evaluation block on `scope_evaluations_for_role` itself.
- **e2e flow is alive.** `setup_e2e.py` + mock LLM + backend: upload → inspect → patch → confirm → import → audit trail (`inspect_upload` readable, `patch_upload_rows`/`import_personnel` as confirmed writes) → double-confirm 409: "E2E API FLOW: ALL PASSED".
- **An eval harness already exists** — refuting part of the prompt's suspicion (c): `tests/copilot_cases.py` holds 48 realistic Persian intents across all five roles, and `tests/test_copilot_eval.py` asserts per-role advertisement, risky→card-not-execution, Persian style rules that admins cannot strip, injection framing, the 30 KB schema budget, and that no read path answers 500. Its docstring states honestly what it does *not* measure (model choice).

## Could not check, and why
- **Real-model tool-selection accuracy**: no LLM API key in this environment. My measurement (Proposals context) is a lexical router over the *advertised* surface — the same caveat the repo records in test_copilot_eval.py:8-17 and docs/open-findings-ai.md بخش «پ»; the clusters I reproduced match the repo's own "~50% lexical wrong-routing" estimate.
- **True concurrent confirmations**: the atomic-claim logic is proven by construction and by the sequential 409, but a real parallel load test needs a multi-process client (the repo notes the same gap).
- **Browser e2e** (`run_e2e.sh` full mode): no display in this environment; I drove the `--api-only` scenario manually with the backend venv (the stock script invokes bare `python3`, which lacks deps here — an environment quirk, not a repo bug).
- **Persian collation in a production install**: this environment's PostgreSQL has ICU and all tests pass; behavior on a non-ICU build is out of scope here.

## Proposals — max 8, ranked, outcome first
Ground truth for ranking: 7 of the prompt's 8 suspected gaps shipped in 1.12.0–1.14.0 (cost ledger, stale-case digest, rolling summary + 20 attachments, diff cards, offline harness, rules_text, Persian style). These proposals are what is still absent.

1. **"HR asks in Persian, the copilot stops picking the wrong tool" — make the wrong-tool rate a measured number.** Outcome: dead-end answers ("هیچ ابزاری…") become rarer and every prompt change becomes provably better or worse. Change: add a `--live` mode to the existing 48-case harness that sends each intent through the configured LLM endpoint and scores tool choice with the already-written style checker. Files: `tests/test_copilot_eval.py` (+ a small runner in `e2e/`). Breaks: nothing offline; requires the admin's API key to run. This is the one measurement the repo itself lists as never taken.
2. **"The card I confirm describes today, not yesterday."** Outcome: the human confirms the real before→after even when the world moved between proposal and confirm (caps edited, chain repointed). Change: recompute `preview_of` at confirm-render and show it beside the stored snapshot, flagging differences. Files: `api/routers/ai.py:_pending_read`, `services/ai/confirmations.py`, `frontend/src/components/copilot/Cards.tsx`. Breaks: nothing; the stored snapshot stays for history.
3. **"Every refusal an auditor can query."** Outcome: the audit log answers "what did the copilot try and who stopped it" with zero blind spots. Change: log proposal (`ai_action_proposed`) and confirm-time blocks (`ai_tool_refused: confirm_blocked`), and mark blocked rows failed. Files: `services/ai/orchestrator.py`, `services/ai/confirmations.py`. Breaks: nothing (append-only events).
4. **"Data cannot pose as instructions anymore."** Outcome: injected names/comments/spreadsheet cells can no longer forge the frame or arrive unframed through tool results. Change: neutralize fence runs (≥8 U+2500) inside `_untrusted` bodies and add one sentence to `_PROTOCOL_RULES` declaring tool results data. Files: `services/ai/prompt.py`. Breaks: none (cosmetic for legitimate data).
5. **"Users see their own meter before the 429."** Outcome: an HR user knows "۸ پیام از سقفِ روزانه مانده" instead of hitting an opaque daily block; per-user cost awareness without admin rights. Change: return own usage/remaining in `GET /api/ai/status` (data already sits in `AiUsageLog`) and a one-line chip in the copilot header. Files: `api/routers/ai.py`, `schemas/ai.py`, `frontend/src/components/copilot/CopilotPanel.tsx`. Breaks: nothing (read-only, own data).
6. **"Pending cards don't rot in silence."** Outcome: a card nobody decided for hours gets one gentle nudge in its own conversation (same pattern as the shipped stale-case digest), instead of surviving as a dead button until TTL. Change: extend the existing scheduled sweep to emit an aging note for `pending` rows older than N hours. Files: `services/scheduled.py`. Breaks: nothing; writes only assistant messages.
7. **"Recurring imports stop costing a re-upload."** Outcome: the monthly corrected workbook can be re-staged from a committed upload's bytes as a fresh draft (HR picks "وارد دوبارهٔ همین فایل"), instead of hunting the original file. Change: add a tool/endpoint that copies an old `AiUpload.content` into a new un-committed staging row. Files: `services/ai/tools/uploads.py`, `api/routers/ai.py`. Breaks: nothing (new staging row; all import guards re-run).
8. **"Sharper tool menu, paid for only if the measurement says so."** Outcome: lower prompt cost per turn and better selection, with a before/after number instead of taste. Change: once Proposal 1 produces a live wrong-rate, compress tool descriptions to ≤60 chars and re-run the same harness; keep the change only if the rate holds. Files: description strings in `services/ai/tools/*.py`. Breaks: nothing; the 30 KB budget test already guards the size.

*Proposals deliberately exclude: staged disclosure/router (rejected by the repo with a measurement — reviving it without Proposal 1's number would repeat the exact mistake the budget-test comment warns about), RAG/pgvector for regulations (`ai_settings.rules_text` already covers this scale; the repo's revisit condition of >20 pages stands), and anything touching `snapshot.py`, score math, or the approval chain (none needed).*

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 05 REVIEW
# ══════════════════════════════════════════════════════════════════

# P05 — Privacy & disclosure review (NexaHR @ 27c83e5)

## Verdict — 3 lines
Privacy core is genuinely well-built: every `suppressed_avg` call site counts *people* and applies the threshold, the HR-panel shield is enforced on list/detail/every write path, self-assessments are HR-only, audit payloads are capability-gated, and no employee-facing route reads another person's data. The one real hole is that the shield is enforced as per-endpoint discipline, not as a central rule — so the dashboard "in-progress" window and the self-assessment invite still see/write the shielded open case of an HR-unit member (all executed live). Three smaller parity defects (copilot HR queue vs UI list, copilot context people-scope, existence leaks on secondary surfaces) round out the list; nothing on the ALREADY FIXED list regressed.

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. MEDIUM — dashboard.py:575-612 (`personnel_in_progress`) — the only HR-panel window with no shield and no own-case guard.**
What breaks: the endpoint returns an *open* record (`IS_OPEN_RECORD`, line 589) filtered only by `_can_view_personnel` (line 583), which is `True` for every HR user (sees_all_personnel) — so HR reads the shielded in-flight case of an HR-unit colleague: `evaluation_id`, `evaluation_code`, exact `status`, `was_returned` (queried from AuditLog, line 595-605) and `created_at`. Every other surface refuses this exact record: the list filters `~IS_SHIELDED_FROM_HR_PANEL` + own case (evaluations.py:637-640), detail raises 403 via `ensure_hr_may_handle` (evaluations.py:190, self_evaluation.py:218-229), and all HR writes sit behind the same guard. Polled repeatedly, the endpoint is a live tracker of a teammate's chain progress. Cross-check: `radar`/`trend` (524-568) share the guard but are finalized-only, so the shield (open-only by definition, workflow.py:151/154) correctly does not apply to them.
How to reach (executed): role=hr viewer; subject in an `is_hr_unit` org unit with an open `hr_review_skipped=True` case → `GET /api/evaluations` hides it, `GET /api/evaluations/{id}` → 403, but `GET /api/dashboard/personnel/{id}/in-progress` → 200 `{evaluation_id: 1, evaluation_code: "EVL-9001", status: "draft", was_returned: false}`. An HR user also sees their *own* open case here (no own-case exclusion, contrast evaluations.py:640).
Fix: after the `_can_view_personnel` check add `ensure_hr_may_handle(record, current_user)` (import from `services/self_evaluation.py`) — it returns silently for non-HR roles, so employees/supervisors keep their path; shielded and own cases then read `null` like everywhere else.

**2. LOW — shielded-case existence/state leaks + an HR write on the shielded case (three surfaces, one root: "open record" checks that ignore `IS_SHIELDED_FROM_HR_PANEL`).**
What breaks: the shield's promise ("تا پیش از ثبت نهایی از پنل منابع انسانی قابل دسترسی نیست", self_evaluation.py:226-228) is broken by:
- personnel.py:230-247 (`_with_accounts`): `open_evaluation_id` + `self_assessment_state` on every personnel row — `state=pending/invited` additionally reveals the case is still in `draft`; `open_evaluation_id` is the shielded record's id.
- dashboard.py:487-494 (`expiring_contracts`): `has_open_evaluation=True` for a shielded subject — HR's contract screen discloses that the colleague has an open case.
- personnel.py:282-303 (`invite_to_self_assessment`) + services/self_assessment.py:146-204 (`invite`): no shield check — HR *writes* `self_assessment_invited_at` onto the shielded record (an out-of-chain HR effect, exactly the class the shield exists to block per workflow.py:137-141) and the response echoes the shielded record's `open_evaluation_id`; the copilot twin `invite_self_assessment` (ai/tools/evaluations.py:479-499) additionally returns the case's `evaluation_code` in its payload. Same pattern again in ai/tools/people.py:120-150 (`_person_payload` → `open_evaluation_id`), code-verified.
How to reach (executed): HR viewer; HR-unit member with active account and open shielded case → `GET /api/personnel` shows `open_evaluation_id=1, self_assessment_state="pending"`; `GET /api/dashboard/expiring-contracts` shows `has_open_evaluation=true`; `POST /api/personnel/{id}/invite-self-assessment` → 200 with `open_evaluation_id=1` and the record's `self_assessment_invited_at` set in DB.
Fix: add `~IS_SHIELDED_FROM_HR_PANEL` to the open-record subqueries in `_with_accounts` (personnel.py:236-243), `expiring_contracts` (dashboard.py:487-493) and `_person_payload` (tools/people.py:120-124), and raise the same 403 wording as `ensure_hr_may_handle` in `self_assessment.invite` when `hr_panel_is_shielded(record)`.

**3. LOW — ai/tools/evaluations.py:530-532 (`my_open_cases`, HR branch) — queue drifts from the UI list: no own-case exclusion, no shield filter.**
What breaks: the branch is `status == submitted` only, while the UI queue for the same role is `submitted AND ~IS_SHIELDED_FROM_HR_PANEL AND subject != user.personnel_id` (evaluations.py:625-640); the comment claims "همان چیزی که فهرست رابط هم نشان می‌دهد". Shielded cases cannot reach `submitted` (all five transitions into `submitted` guard on `not skips_hr_review`, workflow.py:179-297 — code-verified), so the reachable defect is the HR user's *own* case: an hr-role account whose `personnel_id` sits outside the HR unit has a normal case that the UI queue hides but the copilot queue shows (code + status + subject name via `_describe_record`), and the UI's own rationale (evaluations.py:626-634) is that the queue is the window to evidence the subject must not read pre-announcement.
How to reach (executed): hr-role user linked to non-HR-unit personnel, own case `submitted` → `my_open_cases` count=1 with the own case present; `GET /api/evaluations` for the same user → own case absent.
Fix: in the HR branch add `& ~IS_SHIELDED_FROM_HR_PANEL` and `& (EvaluationRecord.subject_personnel_id != ctx.user.personnel_id)` (or route the branch through `scope_evaluations_for_role`). Sibling drift already reported for the CEO branch (`deputy_approved` only vs `IS_ON_CEO_DESK`) — queue completeness, not a leak (P02#3).

**4. LOW — ai/context.py:42-67 (`_visible_personnel_ids`) — missing the record-seat union its twin got, so the model text under-reports the user's real scope.**
What breaks: the twin in ai/tools/people.py:46-82 unions `EvaluationRecord` seats (71-79) on top of `EvaluationAccess` rows; `context.py` unions only the access rows (57-63) + own id. A holder of a record seat whose access row names someone else — legal via `may_act_at` over-rank or after a chain change — is allowed by `_can_view_personnel` (personnel.py:118-149, record-seat clause) and visible to `search_personnel`, but absent from the copilot context people block and from the "پرسنل در دامنهٔ دید شما" count (context.py:107-123, 169-179). Same question, two answers — the exact parity class this repo keeps re-breaking; the evaluations block of the same file *does* use the real `scope_evaluations_for_role` (context.py:134-148), so the file disagrees with itself too.
How to reach (executed): deputy-role user seated in `unit_supervisor_user_id` of a record whose access row lists another supervisor → `_can_view_personnel=True`, person in tools ids, person NOT in context ids (DRIFT=True).
Fix: have `context._visible_personnel_ids` call the people-tools implementation (or move it to `services/authorization.py` next to `sees_all_personnel`); extend `tests/test_ai_people_parity.py` to lock the context copy as well.

## Verified correct
- **Cohort suppression at every call site.** `privacy.cohort_size` is `count(distinct subject)` (privacy.py:42) and every threshold consumer passes a *people* count: reports.py:139/147/177/255/263/344, dashboard.py:199/240/280/436, analytics.py:117-124/170/258/272. Group stats are suppressed on all three routers; deliberate exceptions are all "the user named the data or made it": per-evaluator stats suppressed, per-indicator suppressed, site aggregate thresholds on people while the mean stays record-weighted (analytics.py:294-307, the decided behavior), `employee_vs_unit` unit average suppressed while the named employee's own series is not (reports.py:351-356), `dashboard.overview` overall average unsuppressed but the page is HR-only and already carries person-level rows, and `analytics.my-scoring` suppresses only the *other* side (`org_avg`, `org_share_pct`, per-indicator `org_avg`) — all consistent with the module's own contract. The Excel report prints an explicit "— (جمعیت کمتر از حد نمایش)" cell instead of a silent blank (excel.py:343-347).
- **HR-panel shield on the evaluations surface.** Row check ≡ query filter (workflow.py:151 vs 154); applied at list (evaluations.py:637), detail (190), hr-approve (999), return (1148), claim (1513), handover (1532), add_comment (1823), resolve-objection (`_ensure_can_resolve_objection`), cancel/extend/reassign via `ensure_may_administer` (1247/1302/1639), improvement plans (improvement_plans.py:239), and inside `apply_transition` itself for hr transitions (workflow.py:594-601). Shield is correctly *open-only*: finalized/cancelled HR-unit records, radar/trend, overview `lowest_by_person`, reports and PDFs are visible to HR by the documented design (workflow.py:143-146).
- **Notifications respect the shield.** `_hr_queue_ids` excludes the subject (notifications.py:154-176); `submit_hr_subject`/`*_hr_subject` returns route around the HR queue; vacated-seat notices split shielded (→ deputy/CEO of that case) from unshielded (→ HR) recipients (notifications.py:306-336); shielded cases never sit in `submitted`, so SLA sweeps and the stale-case digest never name them to HR (scheduled.py:104-181, code-verified via TRANSITIONS).
- **Who can read whose evaluation.** `scope_evaluations_for_role` is a fail-closed allowlist shared by list/export/search (evaluations.py:613-666, 781); detail, comments, replies and PDF go through `_ensure_can_view`/`ensure_hr_may_handle`; `_not_found()` merges 404/403 so ids are not enumerable (evaluations.py:116-130); employee list branch is a hard empty page (710-711); subject reads only via `/api/me` or the visibility-gated PDF (evaluations.py:1725-1775); verify endpoint is token-random, finalized-only, and rate-limited per-IP (verify.py:17-24). Copilot `get_evaluation`/`search_evaluations` call the same guards (tools/evaluations.py:89, 146) and scoring stays form-only (no score write tool).
- **Self-assessment visibility.** Viewer set is exactly `{hr}` with all switches removed (self_assessment.py:91, 103-118); detail embeds it only for HR (evaluations.py:276-280); the subject reaches it only through `/api/me` on their own record (`_my_record_or_404` returns 404 for others, me.py:325-355); invitation requires an open record and an active account; no notification ever tells the scorer what the employee wrote (me.py:299-305).
- **PII destinations.** Audit payloads (reasons, bonus, objection text) are only readable in full with `view_audit_log`; without it the route allowlists system events *and* forces `evaluation_record_id IS NULL` (audit_log.py:39-85, 194-201); every Excel export is logged (`excel_exported`, `report_excel_exported` with filters/row count, `personnel_excel_exported`, `audit_log_excel_exported`), formula-injection is neutralized at the sheet layer (excel.py:83-113) and row caps + in-file truncation notes exist. Server logs record path *without* query string (main.py:124-131), so searched names don't land in logs; the delivery logger prints only ids/errors (delivery.py:179-192); console channel is default-off and explicitly labelled dev (config.py:193); external sends are limited to action-required/final types (delivery.py:35-55) with escaped SMS templating (http_sms.py:25-42).
- **Employee-facing views.** `/api/me/*` is `require_own_personnel` + own-record-or-404 everywhere; improvement-plan list scopes non-HR viewers to `owner_user_id` (improvement_plans.py:192-216); personnel list/exports and access chain are HR/`manage_personnel`-gated; `role_overview` self cards are double module-gated (dashboard.py:634-651, 860-866). No cross-personnel read found on any employee-reachable route.

## Could not check, and why
- **Full suite rerun** — not rerun per instructions; 1542 passed already logged (/tmp/pytest_backend2.log), which includes the privacy suites (`test_cohort_counts_people`, `test_cohort_suppression`, `test_evaluation_existence_leak`, `test_hr_own_case`, `test_employee_self_view`, `test_subject_result_gate`) that back the pairs above.
- **Frontend rendering of suppressed/visibility flags** — backend is the server truth and all my probes hit the API; the React layer was only spot-read, not executed (vitest/tsc green per worklog).
- **Real outbound delivery** — no SMTP/SMS service configured here; channel code was read, not live-sent. The e2e/mock path was exercised by the P04 agent, not re-run for this angle.
- **Real-model context leakage** — `context.py` content only matters once an external LLM receives it; no API key available, so "what the model does with it" stays untested (the repo's own documented gap).
- **`EVENT_LABELS` three-way lock beyond the suite** — I read the consumers but did not independently re-derive that every emitted event type has a frontend label beyond trusting the green `test_audit_event_labels.py`.

## Proposals — max 6, ranked
1. **One policy for "does this HR-unit colleague have a case in flight?"** Today HR gets *contradictory* answers on one screen: list/detail say nothing, in-progress/personnel-list/expiring-contracts say "yes, here's the id and stage", invite says "I'll write on it". Outcome for HR: the shield becomes a single understandable rule ("در جریانِ هم‌تیمی‌ام را نمی‌بینم — نهایی‌شده را می‌بینم") instead of four endpoints disagreeing. Change: apply `~IS_SHIELDED_FROM_HR_PANEL` + own-case exclusion at the four sites in Findings 1-2 (dashboard.py:583-592, personnel.py:236-243, dashboard.py:487-494, self_assessment.invite). Cost: 4 files + 2 test files. Breaks: nothing functional; the self-assessment invite button disappears for HR-unit members (deliberate — deputy/CEO can invite from their own path if ever needed).
2. **Copilot queue reads the same allowlist as the UI queue.** Outcome: HR never hears "پرونده‌ای روی میز شماست" from the assistant about a case the panel refuses to open. Change: HR branch of `my_open_cases` reuses the two where-clauses of `scope_evaluations_for_role` instead of hand-rolled `status == submitted`. Cost: 1 file (ai/tools/evaluations.py) + the parity test. Breaks: nothing; employee/CEO branches untouched.
3. **Unify `_visible_personnel_ids` in one place.** Outcome: a supervisor seated on a record by over-rank gets the same copilot context as their pages — no more "I can open him but my assistant doesn't know him". Change: move the people-tools implementation (with the record-seat union) to `services/authorization.py` and have `context.py` import it. Cost: 2 files + parity test. Breaks: nothing — it only widens the under-scoped context copy to match real access.
4. **Replace the accidental shield leaks with one deliberate aggregate.** HR legitimately needs capacity planning, not case files: a single card "N پروندهٔ بازِ واحد منابع انسانی — رسیدگی با معاونت/مدیرعامل" on the HR dashboard would satisfy the need the leaked flags currently (accidentally) serve, without naming anyone. Change: one count query with `IS_SHIELDED_FROM_HR_PANEL` in `dashboard.overview` + one schema field. Cost: 2 files. Breaks: nothing.
5. **Production guard for `NOTIFICATION_CHANNEL_CONSOLE`.** One wrong env var in production pipes employee names, case codes and recipient addresses into server logs forever. Outcome: impossible to misconfigure silently, same as the existing four `_forbid_*` production validators. Change: a `model_validator` rejecting `notification_channel_console=true` when `environment == "production"`. Cost: 2 files (config.py + a test). Breaks: any staging box that runs production-mode with the console channel — it must wire SMTP/SMS, which is the intent.

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 06 REVIEW
# ══════════════════════════════════════════════════════════════════

# PROMPT 06 — Data Integrity & Migrations (Mode: defects)

Review executed against `main` @ 27c83e5. Environment: PostgreSQL 16.4 compiled **with ICU** (source build), migrations replayed from empty twice (`nexahr_test` for the 1542-test suite run, `nexahr_main` scratch for this review); all probes below were executed live against the scratch DB and dropped afterwards. The prompt's counts are stale: the repo has 63 migration files (not 57/58) and 1542 backend tests (not 1131) — noted, not reported as findings.

## Verdict — 3 lines
- The database layer is clean: 63 migrations replay from empty with no errors, `compare_metadata` (the honest equivalent of `alembic check` on Alembic 1.14, which has no such subcommand) reports **zero drift** between models and migrated schema, and all 12 DB enum types match their Python enums exactly in both directions (the one Python-only enum, `EvaluationStage`, is a display helper that never maps a column — by design).
- The append-only audit guarantee held against every attack I executed: UPDATE and DELETE are rejected by the trigger ("audit_log is append-only: … is not permitted"), TRUNCATE is blocked, the hash-chain math in `services/audit.py` is correct (content hash + prev-hash, advisory-xact-locked appends, anchor that only advances after a full successful walk — a live 200-event concurrent-write probe verified the chain cannot fork).
- Zero defects qualify under rule 5. Two design notes below (tamper-evidence scope; one dead partial index) are documented limitations of the design, not broken behaviour — the code's own docstrings already own the first one.

## Findings — severity | file:line | what breaks | how to reach it | fix
None. I could not construct a concrete failing path in this angle. Everything I attacked either held, or is listed under "Verified correct".

## Verified correct
- **Migration replay from empty** — `alembic upgrade head` on a fresh database: all 63 migrations apply cleanly in ~1.4 s, head `f1b6d3e28a47`; the resulting schema is what 1542 green tests run on.
- **Model/schema drift** — `alembic.autogenerate.compare_metadata` against the live migrated DB: empty diff (no missing/extra tables, columns, indexes, or type changes).
- **Enums both directions** — 12 enum types in `pg_type` (`user_role`, `evaluation_status`, `personnel_status`, `period_status`, `scheme_status`, `delivery_status`, `delivery_channel`, `capability`, `comment_stage`, `improvement_plan_status`, `indicator_section`, `separation_reason`) match `app/models/enums.py` value-for-value, no Python-only DB values and no DB-only Python values. `EvaluationStage` (`models/enums.py:44`) exists only to derive a display stage from `EvaluationStatus` in `schemas/evaluation.py:20` and is never a column type.
- **Partial unique indexes vs code assumptions** (the prompt's rule-4 pair):
  - `uq_open_evaluation_per_personnel` (evaluation_records, `WHERE status <> ALL(finalized, cancelled)`) — equivalent to `OPEN_STATUSES` **today**, because the enum has exactly six values; both sides enumerate the same closed set from opposite directions (row filter in `models/evaluation.py:289`, frozenset in `services/workflow.py:34`). Caveat for the owner, not a defect: if a seventh **terminal** status is ever added to the enum, `OPEN_STATUSES` and this index must both be touched or the index will treat the new terminal status as "open" and wrongly enforce one-record-per-person against it. The pair is comment-locked in `services/privacy.py:31`.
  - `uq_single_open_period` (evaluation_periods, `WHERE status='open'`) — matches the singleton-open assumption at `routers/periods.py:65,77,248` and `services/bulk_evaluation.py:243`.
  - `uq_single_active_scheme` (scoring_schemes, `WHERE status='active'`) — matches `services/scoring_scheme.py:89` (singleton lookup) and `:204` (activation).
- **Append-only audit table** (executed): `UPDATE audit_log …` → blocked (`InsufficientPrivilege: audit_log is append-only: UPDATE is not permitted`); `DELETE …` → blocked the same way (my first probe *appeared* to delete — it was a no-row DELETE on an empty table, a probe artifact, not a bypass; with a row present the trigger fires); `TRUNCATE` → blocked; `INSERT` with NULL hash columns → blocked by NOT NULL. Triggers: `trg_audit_log_append_only` (row-level UPDATE/DELETE) + `trg_audit_log_no_truncate` (statement-level TRUNCATE), migration `e8f4b127d905`.
- **Hash chain logic** (`services/audit.py`) — `_canonical` sorts keys and excludes `created_at` (server-assigned at INSERT; the trigger, not the hash, protects it — the exclusion is documented at `audit.py:54`); `log_event` serializes concurrent appends on `pg_advisory_xact_lock(:key)` before reading the tail (`audit.py:87`), so the chain cannot fork (a separate concurrency agent's 8×25 parallel-write probe ended `verify_chain ok=True, checked=200`); `verify_chain`'s three modes (full / tail-window / since-anchor) all share `_walk`, the anchor is re-verified against the log row before use (`audit.py:188`), and `record_full_verification` is the only place the anchor advances and only after a fully successful walk (`audit.py:280-330`) — the "fast green" blind spot is correctly closed.
- **Cascades / orphans on delete** — `delete_user` (`routers/users.py:353`) refuses any account with audit history (409, with the reason), cleans its direct dependents, and wraps the final delete in `except IntegrityError` so the *database*, not a code-maintained list, is the source of truth for "is this account referenced"; `audit_log.actor_user_id` is deliberately RESTRICT (an actor is never hard-deleted — enforced, and my probe confirmed the FK blocks it); AI tables use CASCADE/SET NULL correctly (`ai_user_access`, `ai_conversations`/`ai_messages` CASCADE; `ai_usage_log` SET NULL); `delete_org_unit` refuses units with personnel (string-count guard, `org_units.py:175`); `org_unit` on `personnel` is a deliberate free-string (owned by the docstring at `models/org_unit.py:5-16`) and the rename endpoint moves all matching personnel rows in one UPDATE (`org_units.py:138-144`) — a test-quality agent separately verified that cascade is untested (their finding, not a data-integrity break).
- **String-vs-enum comparisons** — every `status`/`role` comparison in `app/` is typed against an enum member (sweep across routers/services); the only raw-string status comparisons live on genuine `VARCHAR` columns whose written values match exactly: `SchedulerRun.status` (writes `skipped_locked/running/succeeded/failed` at `services/scheduler_lock.py:65-84`; compares `admin.py:33`, `readiness.py:60`), `AiPendingAction.status` (writes `pending/executing/confirmed/failed/expired/rejected` at `services/ai/confirmations.py:55-216, orchestrator.py:259`; compares the same strings, always conjunct with the row id), `AiMessage.role == "assistant"` (`api/routers/ai.py:293`).

## Could not check, and why
- **Alembic downgrade paths** — the prompt asks for replay from empty, not round-trip; downgrade of all 63 migrations was not exercised (nothing in the product's workflows downgrades; risk accepted).
- **Tamper-proofness of the audit trail** — by the design's own admission (`audit.py:14-20`): this deployment has a single DB role, so the role that *owns* the table can `ALTER TABLE … DISABLE TRIGGER` it — I executed exactly that successfully as the app role (and re-enabled it). The hash chain makes tampering *detectable* only if the verifier's anchor is out of the attacker's reach; a party holding both DB and code can rebuild the chain. The docstring names the missing piece (an external append-only sink) as an undecided infrastructure item. Design objection, not a bug: worth deciding, not fixing in code.
- **`ix_evaluation_records_open_objection`** — a non-unique partial index (`WHERE objection_at IS NOT NULL AND objection_resolved_at IS NULL`, migration-built) with **no consumer query anywhere** in `app/` or the frontend: objections are resolved per-record by id, and no list/queue/dashboard filters on open objections. Nothing fails (rule 5) — it is dead weight on writes today, or scaffolding for an objection queue that does not exist yet. The owner should either delete it or build the queue it anticipates.
- Historical migration-by-migration enum-value evolution (each intermediate state) — the final state is proven consistent by the live diff; replaying 63 intermediate states individually would add evidence about *transitions*, not about what runs today.

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 07 REVIEW
# ══════════════════════════════════════════════════════════════════

P07 — Concurrency & scheduling review (Mode: defects)
Repo: /home/z/my-project/NexaHR @ 27c83e5 (branch main) — read-only; probes ran on scratch DB `nexahr_p07` (created, migrated to head, dropped after).
Angle: `with_for_update` on every read-decide-write path, `pg_advisory_xact_lock` in the audit chain, `scheduler_lock.py` + `services/scheduled.py` sweeps, the outbound delivery queue, double-submit / double-confirm, and `db.commit()` boundaries in endpoint bodies.

## Verdict — 3 lines
The concurrency core is unusually well-defended: every workflow/HW/me/plan write path reads its row `FOR UPDATE`, double-submit and double-confirm are backed by DB constraints or atomic conditional claims, the audit chain serializes on a transaction advisory lock, and the scheduler leader-lock is taken and released on a dedicated connection — all verified by execution (probes A/B below) plus the repo's own two-connection tests, no regressions of the ALREADY-FIXED list found. Three defects survive, all LOW and all in the periphery of the guarded core: `close_period` is the one period mutation that did not get the lock its own sibling `delete_period` documents as mandatory (a case can be born under a period that a concurrent close just shut — executed), `self_assessment.invite` is an unlocked read-decide-write on the record (executed), and the delivery sweep's send-before-commit boundary is at-least-once, contradicting the module's "دقیقاً یک‌بار" claim (UNVERIFIED, analysis only). Nothing above LOW; no finding touches the hashed legal document path.

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. LOW | backend/app/api/routers/periods.py:247 (close body 238–292) + backend/app/api/routers/evaluations.py:452–454, stamped at :468 | A draft evaluation can be created under an evaluation period that a concurrent HR request just closed — exactly the state the close guard's 409 message refuses to create without `force`.** `close_period` loads via `_get_period_or_404` (periods.py:40–44, plain `db.get`, no `FOR UPDATE`) and counts open cases (:259–266) unlocked; `create_evaluation` reads the open period unlocked (:452–454) and stamps `period_id` at :468. No constraint, trigger or lock exists on this pair. — *How to reach:* HR closes the empty period "X" (`POST /api/periods/{id}/close`) at the same moment a unit supervisor opens a case (`POST /api/evaluations`); the supervisor's read of the open period lands before the close commits, the insert commits after it → an open draft lives under a closed period, is finalized under it later, and counts in `period_progress` of a closed period. *Executed:* probe C on `nexahr_p07` (session A reads period as `open`, session B closes+commits, session A inserts+commits) → record 2 persisted with `period_id` of a period now `closed`. The repo itself states the lock is required for this exact class of race in `delete_period`'s docstring (periods.py:184–191: "تا بینِ شمارش و حذف کسی پروندهٔ تازه‌ای روی همین دوره باز نکند") — close simply never got it. — *Fix:* `.with_for_update()` on the period row in `close_period` (mirror lines 192–194), and take the same row lock in `create_evaluation`/`bulk_evaluation.execute` (bulk_evaluation.py:242–244) before stamping `period_id`.

**2. LOW | backend/app/services/self_assessment.py:158 (read via `open_record_for`), :167 (stale check), :181–182 (write) | `invite` is a read-decide-write on the evaluation record with no row lock, so `self_assessment_invited_at`, an official audit event (`self_assessment_invited`) and a "فرم خودارزیابی این دوره برای شما باز است" notification can all land on a record whose self-assessment window is already closed.** The employee's own submit path is properly locked (`me.py:211` `for_update=True`) and HR's transition paths are locked, but invite reads through the unlocked `open_record_for` (:121–131) and writes without ever re-seeing the committed status. — *How to reach:* HR clicks "invite to self-assessment" while the supervisor's submit (`POST /api/evaluations/{id}/submit`) commits in between invite's read and write → the employee receives an invitation to a window that no longer exists; the record permanently carries `invited_at` for a phase that ended before it (HR's list then shows an invitation that can never be used). *Executed:* probe D on `nexahr_p07` — invite's read captured, a submit committed, invite's write completed → final state `status=submitted, self_assessment_invited_at=not null`. — *Fix:* load the record with `.with_for_update(of=EvaluationRecord)` in the invite path (same pattern as `me._my_record_or_404(for_update=True)`), so the status check at :167 is taken under the lock.

**3. LOW, UNVERIFIED | backend/app/services/delivery.py:160–197 with commit only at backend/app/services/scheduled.py:583 | The outbound queue is send-before-commit: `channel.send()` executes mid-transaction while the `sent`/`failed`/`abandoned` status writes only persist at the single `db.commit()` that ends `run_all_sweeps`.** A crash (or commit failure) after one or more successful sends but before :583 rolls the status rows back to `pending` → the next sweep re-sends the same SMS/email to the same recipients. — *How to reach:* process kill (deploy restart, OOM) or a DB error at commit during a delivery sweep that already sent a batch (default batch 50, `delivery_batch_size`, config.py:195). Not executed — proving it needs fault injection at the commit line, which I did not attempt; the interleaving is read directly from the code (no intermediate commit exists between `channel.send` at delivery.py:163 and scheduled.py:583). — *Fix:* commit each delivery row's outcome in a short transaction immediately after its send (`db.commit()` inside the loop, the session is otherwise idle), or invert to send-after-commit with a `sending` marker; at minimum, correct the "اجرای «دقیقاً یک‌بار»" claim in scheduler_lock.py:1 — the delivery leg is at-least-once by construction.

## Verified correct

- **Row locks on every read-decide-write path.** All nine workflow endpoints (submit, hr-approve incl. `hr_finalize_direct_ceo`, deputy-approve, ceo-finalize, return, cancel, extend-submission, hr-claim, hr-handover, reassign, resolve-objection) and the scorer writes (scores upsert, evaluator-comment, special-score, top-level comments incl. implicit HR claim) load via `_get_record_or_404_for_update` (evaluations.py:140–158) — verified by reading every endpoint body (lines 940–1706, 1800–1884). `me.py` mirrors it (`_my_record_or_404(for_update=True)`, me.py:341–346) for self-assessment submit and objection filing; `improvement_plans.py:72–77` for plan writes; `personnel._close_out_departure` locks the open record before cancelling it (personnel.py:584–591) — so a concurrent ceo-finalize cannot be over-cancelled. **Executed (probe B):** two parallel `POST /hr-approve` from independent sessions/connections → `[200, 400]`, exactly one `hr_approved` audit event, final status `hr_approved`.
- **Double-submit (two parallel case creations).** Pre-check + partial unique index `uq_open_evaluation_per_personnel` + `IntegrityError` → 409 with the winner's id (evaluations.py:434–496); the bulk path adds per-row savepoints and reports `blocked_conflict` honestly (bulk_evaluation.py:283–297).
- **Double-confirm (copilot).** `_claim_for_execution` is an atomic conditional `UPDATE … WHERE status='pending'` committed immediately, before any tool runs (ai/confirmations.py:53–61, :108) — a parallel confirm gets 409 even mid-execution; failure paths roll back partial writes then record `failed` + audit (confirmations.py:124–158). Covered by tests/test_audit_fixes.py:578, :419, :456.
- **Session rotation.** `rotate_session` reads `FOR UPDATE OF AuthSession` with `populate_existing` (sessions.py:103–108); reuse outside the 60 s grace revokes the whole family (sessions.py:120–125) — the two-tab refresh case stays inside grace by design.
- **Lockout counters.** `record_failure` is upsert (`on_conflict_do_update` with a no-op SET) + `FOR UPDATE` + `populate_existing` (login_guard.py:76–93) — no lost counts toward the lockout, no `UniqueViolation` on the first concurrent insert; failure counting, audit and HR notification commit in one transaction (auth.py:132–149). ALREADY-FIXED item holds; test_login_guard_concurrency.py passes.
- **Audit chain.** The only `AuditLog(` construction in the app is inside `log_event`, which takes `pg_advisory_xact_lock` before reading the previous hash (audit.py:87–109), so the chain cannot fork; UPDATE/DELETE are blocked by the DB trigger (audit.py:13, migration e8f4b127d905). **Executed (probe A):** 8 threads × 25 `log_event` on independent connections → `verify_chain` `ok=True, checked=200`. The interactive check is read-only (`since_anchor` path); the anchor advances only in `record_full_verification` (audit.py:280–330), called solely from the leader-locked sweep (scheduled.py:473).
- **Scheduler double-run safety.** `run_sweeps_once` holds `pg_try_advisory_lock` on a *dedicated* connection for the whole sweep and unlocks on that same connection (scheduler_lock.py:57–98 — the M-10 fix holds, also under the mid-run commits that `run_all_sweeps` performs); losers record `skipped_locked`; a failing sweep rolls work back but still records a `failed` run (:80–91). The manual endpoint goes through the same lock and 409s (admin.py:32–37). Tests test_scheduler_reliability.py:135, :167, :206 and test_audit_fixes.py:741 (lock does not leak across mid-run commits) all pass.
- **Sweep transaction boundaries.** All sweep functions are commit-free; exactly one commit ends the batch (scheduled.py:583); notifications are created in the same transaction as the transition that caused them (notifications.py:1–5, workflow.py:683) and roll back with it. Sweeps never call `log_event`, so the long sweep transaction never holds the audit-chain advisory lock — checked, not assumed.
- **Delivery queue mechanics (minus finding 3's boundary).** Dedup keys include status/review-date/fingerprint; exponential backoff `base 5 min × 2^(attempts−1)` with a 5-attempt cap then `abandoned` (delivery.py:107–119, :176–184); a channel that disappears leaves rows intact; an orphaned notification is abandoned, not retried (delivery.py:153–157); failed rows are retried only by the leader-locked sweep.
- **Other guarded races.** Double-open period prevented by partial index `uq_single_open_period` + IntegrityError → 400 (periods.py:81–89; models/evaluation_period.py:40–41); double-active scheme by `uq_single_active_scheme` (models/scoring_scheme.py:107–112); double document by `evaluation_documents.evaluation_record_id unique=True` (models/evaluation_document.py:20–22), and the background PDF task is idempotent and swallows its own races (documents.py:90–115); `evaluation_code` comes from a PG sequence (evaluation.py:141); `delete_period` locks the row before counting (periods.py:192–194); Excel export builds the workbook before commit (evaluations.py:806–813) and `stage_stats` is window-capped (stage_stats.py:169) — the other two ALREADY-FIXED items hold, no regressions.

## Could not check, and why

- **Finding 3's crash window** — needs fault injection between `channel.send()` and the sweep commit; not attempted on the shared 2-core box, hence UNVERIFIED. Everything else in this report was executed.
- **Live SMTP/SMS channels** — no real channel is configured in this environment; channel implementations were reviewed only as call targets of the sweep, not for protocol-level idempotency.
- **True multi-process scheduler behaviour** — the leader-lock tests simulate the second instance with a second connection (test_scheduler_reliability.py:135) and I did not deploy two uvicorn replicas on this single machine; the lock semantics themselves (session-level, dedicated connection) were verified by code and tests.
- **Frontend double-click protection** — out of scope for this angle; the backend serializes every transition under row locks regardless of what the UI sends (probe B is exactly the double-click case).
- In-process rate-limit counters are ALREADY DECIDED (documented, single-instance) and were not re-opened.

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 08 REVIEW
# ══════════════════════════════════════════════════════════════════

# P08 — Localization review (core/clock.py, UTC/local boundary, Jalali/PDF, Persian digits, RTL CSS, labels)

## Verdict — 3 lines
The UTC/local boundary is genuinely owned by `core/clock.py`: every timestamptz range filter goes through `local_day_start`/`local_day_end`, the PDF's Jalali conversion converts through `to_local` first, and the Vazirmatn/Vazirmatn-Latin font pair covers every character the document can emit (verified per-codepoint with fontTools). No regressions of the ALREADY-FIXED list were found. Two real defects remain, both at *input* boundaries the fixed machinery trusts: the manual personnel path accepts Persian-digit codes verbatim (import normalizes, manual does not — duplicate identity risk), and the frontend's "today" helpers use browser-local components instead of the ORG_TIMEZONE they sit next to, so filter presets and the deadline banner disagree with the server around Tehran midnight for any non-Tehran browser.

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. Medium | backend/app/schemas/personnel.py:12 (+ personnel.py:379,656,76-79; contrast personnel_import.py:105,151) — Persian-digit personnel codes accepted verbatim on the manual path**
- What breaks: `personnel_code: str = Field(min_length=1, max_length=50)` has no pattern and no digit normalization, so «۱۲۳۴» and `1234` are two distinct identities to the DB, while the Excel-import path normalizes both to ASCII (`_DIGITS` translate, personnel_import.py:105, applied at :151). Consequences: the duplicate check (`api/routers/personnel.py:379` create, `:656` update, raw `==` on the code) misses the ASCII twin → the same person can exist twice, and each copy can hold its own open evaluation (the one-open-evaluation index is per personnel row); code search (`personnel.py:76-79` `ilike` on the raw string) cannot find «۱۲۳۴» when HR types `1234` (or vice versa), so contract-expiry and evaluation workflows silently skip that person.
- How to reach it: HR creates a person with a Persian keyboard and types/pastes «۱۲۳۴» as the code (verified executed: `PersonnelCreate(personnel_code="۱۲۳۴", …)` validates and stores `«۱۲۳۴»` verbatim). First visible symptom is earlier still: `suggestUsername(form.personnel_code)` (frontend PersonnelPage.tsx:410) feeds Persian digits into the suggested username, and `UserCreate`'s pattern `^[a-zA-Z0-9._-]{3,50}$` (schemas/personnel.py:33) rejects it — HR gets an account-creation validation error for a "valid" code. Later search/import of the same code as `1234` produces the duplicate identity above.
- Fix: normalize Persian (۰-۹) and Arabic-Indic (٠-٩) digits to ASCII at the PersonnelCreate/PersonnelUpdate boundary — or in the router just before the duplicate check — reusing `personnel_import._DIGITS` so both doors to the unique key agree.

**2. Low | frontend/src/utils/dates.ts:50-61 (+ SubmissionDeadlineBar.tsx:41-45, jalali.ts:197-200) — frontend "today" uses browser-local components, not ORG_TIMEZONE**
- What breaks: `localTodayIso()` builds `YYYY-MM-DD` from `getFullYear()/getMonth()/getDate()` — the *viewer's* timezone — although the same file defines and uses `ORG_TIMEZONE` for display (dates.ts:16,19-27). The backend measures every window with `today_local()` (Asia/Tehran, evaluation_window.py:57, scheduled.py:51,78, dashboard.py:484). Whenever the browser TZ ≠ Asia/Tehran, the two sides answer differently for part of every day. Affected call sites: contract-filter presets (`ReportsSection.tsx:260-261,271`, `AuditLogPage.tsx:264-265,275` — "منقضی‌شده"/"N روز" ranges shift by a day) and the submission-deadline banner (`SubmissionDeadlineBar.tsx:45`, which keeps its own second copy of the same logic).
- How to reach it (executed): froze the clock at `2026-01-05T21:30:00Z` (= Tehran 2026-01-06 01:00) with `TZ=UTC`: `localTodayIso()` → `2026-01-05` while the org day is `2026-01-06`; with `closes_on = 2026-01-05`, the bar shows «مهلت ثبت» (open) because `todayKey > deadline` is false, while the backend rule `today_local() <= closes_on` is closed — the evaluator types a self-assessment and gets a 400 on submit. Browsers east of Tehran (e.g. UTC+8) get the inverse for ~8.5h: banner says «مهلت ثبت گذشته است» while submissions still succeed. `jalali.ts:197-200 todayJalali()` has the same bug with visual-only impact (wrong "today" highlighted in JalaliDatePicker).
- Fix: derive the org day once in dates.ts via `Intl.DateTimeFormat("en-CA", { timeZone: ORG_TIMEZONE })` (the module already imports ORG_TIMEZONE for the formatters), use it in `localTodayIso`, delete SubmissionDeadlineBar's inline copy, and build `todayJalali()` from it.

## Verified correct
- **clock.py owns the boundary as claimed.** `to_local` treats naive datetimes as UTC (the storage convention, clock.py:45-54); `org_timezone()` fails loudly on a bad name instead of silently falling back (clock.py:37-42); `local_day_start/end` verified by execution: `local_day_start(2025-10-15) = 2025-10-14T20:30Z`, `local_day_end = 2025-10-15T20:30Z` (+03:30, no DST since 2022). No `date.today()`, `utcnow`, or naive `datetime.now()` anywhere in `app/` (only the demo seed script and tests).
- **Every timestamptz range filter goes through the local boundary:** evaluations.py:559-563, audit_log.py:104-107, reports.py:68-70 and 304-306, ai/tools/analytics.py:259-264, ai.py:749-751. The only `func.date()` in the app wraps `func.timezone(org_timezone, created_at)` first (ai.py:771) — correct local-day grouping. Date-only columns (contract_end_date, review_date, closes_on, period starts/ends) are compared against `today_local()`-derived dates with no zone conversion — correct, since `Date` columns carry no zone (audit_log.py:133, dashboard.py:484-508, analytics.py:405, scheduled.py:51,402).
- **Jalali in the PDF is day-correct.** `to_jalali` converts through `to_local` before `fromgregorian` (pdf.py:35); executed: `2025-10-06T21:30Z → «۱۴۰۴/۰۷/۱۵ ساعت ۰۱:۰۰»` — the exact off-by-one-day scenario the docstring describes, now right. Invalid/old snapshot strings pass through unchanged instead of crashing (pdf.py:28-31, verified `not-a-date` → passthrough). `fa_date` (core/persian.py:48-60) does the same for notification/error text. Excel renders every datetime/date column Jalali (excel.py:200-201,305,330,399,402).
- **Font loading and coverage.** Template declares both families with explicit files (evaluation_summary.html:31-52); all four woff2 files + LICENSE exist in app/templates/fonts; verified with fontTools that the Arabic subset covers Persian letters, ٪ (U+066A), ٫ (U+066B), ZWNJ, and the Latin subset covers ASCII digits, `+ - / : . = ? & _ ~`, «» and — (U+2014) — i.e., no character the template/fa_digits can emit falls through to Tahoma/DejaVu; `_local_templates_only_url_fetcher` keeps external fetching closed (pdf.py:86-94).
- **Persian digits at output boundaries.** `fa_digits` used in all server-authored text (scheduled.py:82-84,164,317-325,425-427,490; notifications.py:250,310,372; me.py:402; evaluation_window.py:105; evaluations.py:1338,1373; tools/uploads.py:228-230); identifiers deliberately stay Latin (persian.py:36-39) and that policy is applied consistently (evaluation_code/personnel_code raw in PDF, Excel, messages). Decimal point → U+066B, None → «—» not «None». Input side: Excel import normalizes Persian/Arabic digits and strips invisible characters except ZWNJ, with the reason documented (personnel_import.py:105-149). Frontend numerals are uniformly Persian (`fa-IR` Intl in dates.ts:19-27, `toPersianDigits`, `formatPct` Meters.tsx:39-40, `faInt` BulkCreateDialog.tsx:50).
- **Raw enums never reach the user.** Excel label maps match the enum members exactly (excel.py:73-80, 227-233, 269, 272-278 vs models/enums.py — compared member-by-member; `_STATUS_LABELS` and the PDF stage labels are locked by tests/test_audit_category_one.py:168-180); PDF comment stages translate at render time with the raw value only as last resort (pdf.py:104-108, template:227); frontend has exhaustive `Record<Enum,string>` label maps (types.ts:11,42,341,514,627,790) and every badge/table routes through them; `recommendation` is stored Persian text from scheme thresholds (scoring_scheme.py:46-51), printed verbatim in the PDF; the remaining `.value` uses are dedup keys, audit data, metrics labels, and model-facing tool payloads — all data, not display.
- **No stray English reaches the user.** The only English `HTTPException` details sit behind the token-protected `/metrics` endpoint (main.py:224,229 — ops surface); the WeasyPrint-missing path returns a Persian 500 detail exactly as its comment promises (evaluations.py:1761-1769), and the archival sweep degrades to a logged skip + backfill (documents.py:41-67); Pydantic validation arrays are translated to Persian sentences with a field-label table (core/validation_errors.py).
- **RTL/physical-CSS sweep.** Root is `lang="fa" dir="rtl"` (index.html:2); logical utilities (ms/me/ps/pe/start/end) are used where flow-relative; I audited all 91 physical `left/right/pl/pr/ml/mr/text-left/right` sites: each icon↔padding and motion↔direction pair agrees for a fixed-RTL app (PasswordInput pl-11+left-2; SearchInput right-3+pr-9; toggle knob right-0.5↔right-[22px], direction locked by AdministrationPage.test.tsx:335; sidebar right / bell+profile dropdowns left-0 consistent with the sidebar side; slider marker `left: (5-n)/4` matches the RTL-flipped range input, ScoreForm.tsx:330,342; all LTR islands — usernames, emails, codes, verify URL, hash — are wrapped in `dir="ltr"`, often with `text-left`, which is intentional). No visual break found; the only residue is cosmetic (two search-input designs mirror their icon side).

## Could not check, and why
- **Visual rendering in a real browser** (no display/Playwright in this env): the RTL/CSS conclusions are code-audited pair-by-pair, not screenshot-verified.
- **Glyph shaping (joined forms) inside WeasyPrint/Pango output**: PDF bytes were produced and hash-verified in this environment (suite green; P03 re-verified sha256 live), and I verified per-codepoint cmap coverage — but I did not visually inspect shaped Arabic forms in a rendered PDF.
- **A deployment with `ORG_TIMEZONE ≠ Asia/Tehran`**: the frontend hard-codes `"Asia/Tehran"` (dates.ts:16) and test_org_timezone.py:136-137 deliberately fails if the two diverge; I did not stand up such a deployment (single-organization product; the lock is by design).
- **Copilot-generated Persian prose quality** (numerals/dates inside model answers): needs a real model key; the offline harness exists but live-model measurement was out of scope here (matches P04's "could not check").

## Proposals — max 6, ranked
1. **HR never opens an export that prints a raw enum.** The N20 lock (tests/test_audit_category_one.py:168-180) covers excel `_STATUS_LABELS` and the PDF stage labels; extend the same three-line check to `_ROLE_LABELS`, `_PLAN_STATUS_LABELS`, `_SEPARATION_LABELS` (excel.py:227-233,269,272-278), which still rely on the silent `.get(x, x)` fallback. Cost: tests only.
2. **The QR auditor abroad can trust the date at a glance.** The verify page already formats in org time; label it — one «به‌وقت تهران» hint next to «تاریخ نهایی‌شدن» in VerifyPage.tsx:128 removes the exact browser-vs-document doubt the dates.ts docstring (lines 8-12) describes. Cost: 1 file.
3. **HR reads audit rows without a calendar.** old_value/new_value stay raw data (deliberate, persian.py:17-19), but AuditDetails.tsx can *display* ISO-date-shaped values dual-format («2026-01-05 (۱۴۰۴/۱۰/۱۵)»). Cost: 1 file, display-side only.
4. **Archived Excel exports become self-identifying.** A generation stamp row (Jalali timestamp + applied filters) in each workbook means a printed export found in a drawer says when/what it covered — the truncation note (excel.py:34-40) already established the pattern. Cost: excel.py.
5. **One numeral-formatting table in the backend.** core/persian.py is the module, yet excel.py:23, dashboard.py:627 and evaluation.py:147 keep private copies of the same translate table; they agree today only by luck — route them through core so the next separator/rule change cannot drift between a notification and an export. Cost: 3 files.
6. **One search-input design instead of two.** `ui/SearchInput.tsx` (icon at the start edge) and the inline copies in Card.tsx:87-93 / ImprovementPlansPage.tsx:260-274 (icon at the end edge) render the same gesture differently from page to page; converging on SearchInput gives the UI one consistent affordance. Cost: 2-3 files. (Cosmetic; ranked last.)

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 09 REVIEW
# ══════════════════════════════════════════════════════════════════

# P09 — Frontend & a11y (frontend/src/)

Repo: NexaHR @ 27c83e5, mode: defects + elevation. Verification basis: full read of the React app (auth, api, ui, components, pages), byte-level check of suspect lines, oxlint 0/0 on 176 files, `tsc -b && vite build` green, 407/407 vitest green (re-ran focusTrap/Modal suites myself). No backend probe was needed for any finding below; every file:line was read in the current checkout. Note: my tool output strips ANSI-like byte pairs (`[h`, `[m`, `[i`, …) — one suspect "malformed selector" in `focusTrap.ts` turned out to be an output artifact, disproven by extracting the string with a regex; the selector is a valid `a[href], …`.

## Verdict — 3 lines
The boundaries this angle targets hold up: token stays in memory, login *and* logout clear the React Query cache, drafts/copilot state die with the session, PermissionsContext is fail-closed with three refetch triggers, and every module key the UI gates on exists server-side — no cross-user leak path survived inspection.
Four Low findings, all interface-level: five form fields still lack the length guards the repo's own open list already tracks; one Escape press closes every stacked overlay at once; five sub-24px buttons escape the tap-target test's regex; the users-page personnel picker silently caps at 1000.
No Medium/High, no REGRESSIONs of the ALREADY-FIXED list; lint/build/407 tests green.

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. Low | frontend/src/components/employee/OpenCaseCard.tsx:309-314 (+ ImprovementPlansPage.tsx:126-131, ImprovementPlanDetailPage.tsx:292-297, IndicatorsPage.tsx:203-208/211-217 and :406-411/414-419) | Five text fields have no maxLength/counter, so over-long input is only rejected at submit time.** The per-indicator self-assessment note input (OpenCaseCard.tsx:309-314) accepts unbounded text while the server caps it at `SELF_ASSESSMENT_NOTE_MAX=1000` (`backend/app/schemas/evaluation.py:333`, DB column `String(1000)` at `models/self_assessment.py:40`); the sibling *overall* note in the same card has both `maxLength` and `CharCounter` (OpenCaseCard.tsx:325-334), and the mirrored constant `SELF_ASSESSMENT_NOTE_MAX` sits unused at `utils/textLimits.ts:17`. Same gap: plan title (server ≤200, `schemas/improvement_plan.py:32`) at ImprovementPlansPage.tsx:126-131; goal description (≤1000, `schemas/improvement_plan.py:14-16`) at ImprovementPlanDetailPage.tsx:292-297; indicator category/description (≤200/≤1000, `core/text_limits.py:41-42`) at IndicatorsPage.tsx:203-208/211-217 and the edit modal :406-411/:414-419. **How to reach it:** employee → «کارنامه من» → پروندهٔ باز → خودارزیابی → types >1000 chars into any of the 20 «توضیح یا دستاورد شما (اختیاری)» inputs → «ثبت نهایی» → 422 «یادداشت» طولانی‌تر از حد مجاز است (حداکثر ۱۰۰۰ نویسه)» — with 20 note fields the Persian message (validation_errors.py `_field_name` deliberately drops the row index) cannot say *which* note; the employee must hunt. HR variant: add a goal of 1200 chars or paste a long indicator description → same submit-time rejection. Data is safe (server rejects; draft persists in localStorage), but this is exactly the failure the frontend's own `textLimits.ts` header describes ("فرم با سکوتش دروغ می‌گفت") — these five fields are the surviving tail of `docs/open-findings.md` «متوسط ۲» ("maxLength در کل فرانت یک بار به‌کار رفته بود"), which is otherwise fixed (comments/objection/return/cancel/bonus/evidence all have counters now). **Fix:** `maxLength` + `<CharCounter>` from the already-mirrored constants on the five fields; extend the mirror test to assert the constants are actually consumed.

**2. Low | frontend/src/ui/focusTrap.ts:52-58 (used at ui/Modal.tsx:62 and components/copilot/Copilot.tsx:48-50) | One Escape press closes every open overlay at once, not just the topmost.** `useFocusTrap` attaches its Escape handler at the document level and never stops propagation or arbitrates by layer; `Modal` and the floating copilot panel both keep such a trap active whenever open. **How to reach it:** open the copilot panel (bottom-left, always available on every page), then open any modal — e.g. «افزودن پرسنل» or «ساخت برنامهٔ بهبود» — and press Escape intending to dismiss the top layer: both close simultaneously and the half-filled modal form state (plain useState) is gone. Same for PersonPicker's Escape (PersonPicker.tsx:117-119) bubbling into both traps. The repo fixed precisely this class for the date picker (JalaliDatePicker.tsx:107-126 capture-phase listener + `JalaliDatePicker.escape.test.tsx`, whose header describes the identical "کلِ دیالوگ می‌پرید" failure) but the general pair — Modal↔Copilot, Modal↔Modal — still shares it. **Fix:** one place: give `useFocusTrap` a shared layer stack (module-level counter) or capture-phase arbitration so only the topmost active layer handles Escape; `Modal`/`Copilot` call sites stay unchanged.

**3. Low | frontend/src/pages/hr/ImprovementPlanDetailPage.tsx:253 and :270-278, EvaluationDetailPage.tsx:263, UsersPage.tsx:365/:368, Toast.tsx:91 | Interactive controls below the repo's own 24px minimum, invisible to the test that enforces it.** The product's declared standard is WCAG 2.5.8 ≥24px, test-locked in `ui/tapTarget.test.ts` and `index.css:547-553` — but the test only scans buttons whose className carries `text-xs`/`text-[10..13px]`, so these pass CI while measuring under 24px: the goal toggle checkbox `h-5 w-5` = 20×20px (ImprovementPlanDetailPage.tsx:250-265 — *the* primary control of the follow-up owner, who per Layout's own design taps it on a phone), the icon-only delete-goal button with no padding (16px, :270-279), the back button `text-sm` with no padding (~20px tall, EvaluationDetailPage.tsx:263), the row-action buttons «فعال کردن/غیرفعال کردن/حذف» in the accounts table (UsersPage.tsx:365, :368), and the toast dismiss button `p-0.5` ≈20px (Toast.tsx:91). **How to reach it:** any unit supervisor ticking improvement-plan goals on mobile; any HR using table row actions by touch. **Fix:** add `tap-target` (or `p-1.5`) to the five controls and widen the test regex to `text-(xs|sm)` plus icon-only buttons so the next offender fails CI.

**4. Low | frontend/src/pages/hr/UsersPage.tsx:57 | The employee→personnel link picker silently shows only the first 1000 personnel.** `usePersonnelList({ limit: 1000, offset: 0 })` feeds the «پرسنل متناظر» select when creating/editing an account with role «کارمند»; the server accepts `le=1000` without complaint (`backend/app/api/routers/personnel.py:224`), so past 1000 personnel the rest never appear — HR cannot link an employee account to person #1001 and gets no hint (unlike `PersonPicker`, built for exactly this, which prints «نتیجهٔ نخست از total», PersonPicker.tsx:228-233). `docs/open-findings.md` «متوسط ۳» fixed only the *always-fetch* half (the `enabled` gating at PersonnelPage.tsx:368-369); the cap itself remains. **How to reach it:** import 1000+ personnel via the Excel importer (no upper bound there), then open «حساب‌ها» → edit any later employee → the personnel select is missing them. **Fix:** replace the select with the existing `PersonPicker` (server-side `q` search, `accessible_to_me` optional) — one file, component already exists.

## Verified correct
- **Cross-user cache boundaries on shared machines (the angle's #1).** Access token lives in module memory only (`api/client.ts:6-13`), refresh is an HttpOnly cookie; `login()` *and* `logout()` both `queryClient.clear()` (`auth/AuthContext.tsx:54, 74`) — so the second user on the same tab can never render the first user's rows even for a frame; logout additionally deletes every `nexahr:draft:` localStorage key (`ui/useLocalDraft.ts:20-33`) and all SW caches (`pwa.ts:14-22`); a failed 401-refresh falls through to a *full-page* redirect (`client.ts:75-84`), which by construction wipes all in-memory state; copilot conversation state is React-context memory only, mounted inside the Layout shell that returns `null` when logged out (`components/copilot/CopilotSession.tsx:40-71`, `Layout.tsx:82`). localStorage holds nothing but theme, sidebar-collapse, and prefixed drafts; there is no persist plugin on the QueryClient. Retry policy never re-sends 4xx (main.tsx:28-32) — no double-charged rate-limited failures.
- **PermissionsContext fail-closed module gating (the angle's #2).** `isModuleEnabled` defaults to `false` when data is missing or the fetch errored (`auth/PermissionsContext.tsx:58-63`), gated UIs additionally require `!permissionsLoading` (`pages/employee/MyEvaluationsPage.tsx:326-343`); the query is keyed with the user id, enabled only when logged in, refreshed on window focus, every 5 minutes, and on *any* 403 from any endpoint (:66-95) — the repo's own open item «مجوزها هیچ‌وقت تازه نمی‌شوند» is verified closed; `ProtectedRoute` waits for pending permissions instead of wrongly denying capability routes (`ProtectedRoute.tsx:45-47`). Every module key the UI gates on (`periods`, `improvement_plans`, `self_assessment`, `objections`, `employee_evaluation_visibility`, `employee_overview_cards`, `employee_result_acknowledgement`, `role_analytics`) exists in `backend/app/core/modules.py` — zero dead keys, and the server keeps the real enforcement (`ensure_module_enabled` at 8 write/read sites), so UI gating is cover only, as designed.
- **Focus traps and keyboard paths.** `useFocusTrap` is valid and sound: the focusable selector is a correct `a[href], button, textarea, input, select, [tabindex]:not([tabindex="-1"])` (byte-verified — see header note), traps Tab/Shift+Tab, restores focus on close, reads `onEscape` through a ref so parent re-renders don't steal focus (focusTrap.ts:22-84). `Modal` renders via portal with `aria-modal` + `aria-labelledby` that works for ReactNode titles (Modal.tsx:93-100), non-dismissible variant drops Escape entirely; the mobile drawer gets the same trap with `role="dialog"` (Layout.tsx:61-68, 157-180); skip-link to `#main-content` (Layout.tsx:124-129); the forced-password state renders a bare shell so navigation/notifications/copilot are *not rendered at all*, stronger than a scrim (Layout.tsx:84-106). JalaliDatePicker: capture-phase Escape arbitration, roving-tabindex day grid with arrow-key handling, focus return — its three dedicated test files pass. PersonPicker implements the full combobox pattern incl. `aria-activedescendant` (PersonPicker.tsx:58-63, 184) — the repo's open item verified closed. NotificationBell closes on Escape and returns focus to the bell (NotificationBell.tsx:33-37); Toast announcements use `role="alert"`.
- **Form validation vs server rules.** Password: client required rules (≥10, not-username) match the server (`utils/password.ts` vs `backend/app/api/routers/auth.py:271`; generator uses `crypto.getRandomValues` with Fisher–Yates and an ambiguity-free alphabet). Text limits are mirrored (`utils/textLimits.ts`) and value-locked by `test_text_limits.py`; the applied fields all carry `maxLength`+`CharCounter` (comments, replies, evaluator comment, objection, return/cancel/recovery/deadline reasons, bonus reason, evidence). The scoring form validates against the record's **stamped** `scoring_rules`, not today's active scheme (`EvaluationDetailPage.tsx:74-78`) — mid-cycle scheme activation cannot drift form rules vs server (P03's pair, holds); score entry 1..5 is button-driven matching `ScoreInput(ge=1, le=5)`; evidence-required scores and min-word count come from the same stamped config; special score validates 0..scheme-cap + Persian-numeral parsing (`toMachineNumber`, five tests) matching `SpecialScoreUpdate(ge=0, le=100)`; `UsersPage.tsx:80` enforces employee→personnel exactly like `UserCreate`'s validator.
- **Error and empty states.** Every list surface I opened has the trio: inline error line (`EvaluationList.tsx:339-341`), skeletons while pending (:346-352), `EmptyState` (:355) + pagination — same pattern in Personnel/Users/AuditLog/Reports/dashboards/Sessions/detail pages, which use `retry:false` so 403/404 surface immediately; `PersonScorecard` has explicit empty states for no-selection and no-result. `ErrorBoundary` renders a reload CTA (ErrorBoundary.tsx:54-59) and is re-keyed per pathname so a stale error state cannot survive navigation (Layout.tsx:232).
- **Touch targets & contrast (both themes).** The 24px `.tap-target` rule itself is test-locked down to its CSS values, including that `display:inline-flex` survives (tapTarget.test.ts); contrast tests compute real WCAG ratios — `--color-gray-400` ≥4.5:1 on white and `#f9fafb` in light theme and on `#1b2031` in dark (lightThemeContrast.test.ts), with Tailwind 4 mapping `gray-*` to those redefined tokens so all components follow; `theme-init.js` applies theme pre-paint.
- **PWA/service-worker cache.** `public/sw.js` never caches `/api/` or `/verify/` (`isNeverCached`), navigation is network-first with the cached shell only as offline fallback, and the offline fallback for `/verify/` is an honest 503 page rather than a cached document (`offlineResponse`); hashed `/assets/*` are cache-first under a versioned cache, `activate` prunes any older version, `skipWaiting`+`clients.claim`+one guarded `controllerchange` reload prevent mixed-version runs; registration is PROD-only and failure is silent-by-design; a mid-session deploy ends at the ErrorBoundary card whose reload fetches the fresh shell — degraded but always recoverable. The AuthContext comment's claim that SW caches hold no user data is true (only `/`, manifest, one icon are precached).

## Could not check, and why
- **Real-browser/mobile behavior** — no display in this environment (the repo's own `open-findings-ai.md` بخش «پ» records the same limitation): actual focus-trap behavior under a real screen reader, iOS/safe-area quirks, and touch ergonomics are verified by code reading + jsdom tests only; the two Escape/stacked-layer mechanics are code-certain but were not exercised in a live browser.
- **A real deploy double-visit for SW staleness** — no staging nginx here; the strategy is verified from `sw.js` + a local `vite build`, not by actually deploying v2 over v1.
- **Exhaustive per-pair contrast** — the tests lock the token values, not every foreground/background combination (e.g. `pulse-50`/`amber-50` badges over white were spot-checked in index.css, not measured pairwise).
- **A live `/administration/my-permissions` outage against the running backend** — fail-closed behavior is verified in code and unit test (`PermissionsContext.test.tsx`); I judged an HTTP probe redundant and did not start one.
- **Notification polling load (5s visible / 60s background)** — deliberate, documented in `queries.ts:330-345`; measured only by reading, not under real load.

## Proposals — only in `Mode: defects + elevation`, max 6, ranked
1. **«کدام فیلد؟» — خطا زیرِ خودِ فیلد بنشیند.** HR cannot tell *which* input a 422 refers to today (backend already returns structured `detail_items` with field names, explicitly "تا UI بتواند خطا را کنار خودِ فیلد نشان دهد" — validation_errors.py:117-122, unused by the client). Outcome: an employee with one over-long note fixes it in seconds instead of hunting 20 fields; same for the indicator/goal forms. Change: surface `detail_items` through `extractErrorMessage` consumers and render the message next to the matching field in the 3-4 forms that own those fields. Cost: ~4 files, no backend change.
2. **آفلاین باید «آفلاین» بگوید.** The SW promises «پوستهٔ برنامه باز می‌شود و می‌گوید آفلاین» (sw.js header) but nothing in the app ever says it: offline, every query surfaces the generic «خطایی غیرمنتظره رخ داد» toast. Outcome: the supervisor on the plant floor (the stated PWA motivation) understands why nothing loads instead of distrusting the system. Change: one `navigator.onLine` + online/offline listener banner with a retry hint, mounted in Layout. Cost: 2 files.
3. **هزار نفر صدمین: انتخابگرِ سمتِ سرور برای پیوندِ حساب.** An org past 1000 personnel cannot link employee accounts at all (finding 4), and the select will also strain before that. Outcome: HR can link any employee however large the roster grows, with the same keyboard/a11y behavior as the rest of the app. Change: reuse `PersonPicker` for the personnel_id field. Cost: 1 file (component exists).
4. **هر Escape یک لایه.** Closing the calendar, a confirm dialog, or the copilot window must never throw away the half-filled form behind it (finding 2). Outcome: keyboard users stop losing work to a key that means "dismiss this", not "dismiss everything". Change: a layer stack / capture-phase arbitration inside `useFocusTrap` so only the topmost layer answers Escape; `Modal`/`Copilot`/drawer call sites untouched. Cost: ~2 files incl. a stacked-overlay test mirroring `JalaliDatePicker.escape.test.tsx`.
5. **استقرارِ تازه، نیم‌باز را خودش ترمیم کند.** After a deploy, the first click on a never-yet-loaded page of a long-open tab lands on a 404 chunk and shows the error card. Outcome: the app reloads itself once instead of teaching the user to press F5. Change: handle Vite's `vite:preloadError` (or wrap the lazy imports) with a single guarded `location.reload()`, reusing the existing `refreshing`-flag pattern from pwa.ts. Cost: 2 files.
6. **قاعدهٔ ۲۴ پیکسل بی‌استثنا.** The tap-target test's regex misses every sub-24px button that doesn't use `text-xs` (finding 3). Outcome: the next tiny button fails CI instead of reaching a motor-impaired user. Change: widen the rule to `text-sm` and icon-only buttons; fix the five current offenders in the same commit. Cost: 2 files (test + the five class additions).

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 10 REVIEW
# ══════════════════════════════════════════════════════════════════

## Verdict — 3 lines
The cost angle is in unusually good shape: every suspect the prompt names measured a **constant, small query count with data volume** — no N+1 in any read path, and every shipping filter/sort column has a supporting index (EXPLAIN-verified).
Two real cost defects found, both Low and both measured: the truncated-Excel path more than doubles its own worst-case serialization (+122%) to append a one-line note, and `my-scoring` re-scans the whole score table twice per view with no period/window cap — the exact class this repo fixed one endpoint away.
No regressions of the ALREADY-FIXED cost items were found; the frontend bundle is cleanly split (recharts isolated out of the critical path, main chunk 120 kB gzip).

## Findings — severity | file:line | what breaks | how to reach it | fix

**1. Low | `app/services/excel.py:157-174` (+ callers `app/api/routers/evaluations.py:811`, `improvement_plans.py:357`, `personnel.py:357`, `audit_log.py:301`, `users.py:155`) | The truncated-Excel path re-opens and re-saves the entire workbook just to append a one-cell note — measured +122% on the worst case.** `note_truncation` does `load_workbook(BytesIO(content))` + `append(note)` + a second `wb.save` (excel.py:167-174). Executed: at the 5000-row cap, `build_evaluations_workbook` takes **1073 ms** and `note_truncation` another **1312 ms** (+122%); the full request `GET /api/evaluations/export.xlsx` with no filter measured **2036 ms median** (5 queries only — the cost is CPU, not SQL), so ~2/3 of the worst-case export is this second pass. What breaks: HR taking the period-end export blocks one sync request worker **and its DB connection** for ~2 s (file built before `commit` by design, evaluations.py:806-813) — on a small single-instance deploy (2 workers) that is a visible stall for everyone else; and the waste scales with the cap. How to reach: log in as HR → evaluation list → «دریافت خروجی» with no filter while `evaluation_records` > 5000 (measured at 7000). The memory side is fine (230 KiB file) and the 5000-row cap + in-file warning are good design — the defect is only the double serialization. Fix: `cap_rows` already runs **before** the builder (evaluations.py:791-795), so `truncated` is known up front — pass a `truncated: bool` (or the note) into the builder and append the note row before the single `_to_bytes` save; 6 files, one line each, delete `note_truncation`'s reload. Alternative with zero call-site changes: write_only workbook. (Same fix applies to all five builders through `_configure_sheet`.)

**2. Low | `app/api/routers/analytics.py:66-70` (scans at :92-105, consumed at :107-124, :128-137, :155-173, :206-213) | «آینهٔ ارزیاب» (`GET /api/analytics/my-scoring`) takes no period or window parameter and re-scans the entire `evaluation_scores` table twice per view, so its cost grows linearly with org history with no cap.** The `my_scores`/`other_scores` subqueries are each re-evaluated ~4–5× (count, avg, distribution, per-indicator, evidence rate). Measured at the repo's own 3-year benchmark: **14 queries, 159 ms median** — the slowest read endpoint measured (overview 154 ms via the repo's tool, executive 22 ms, report summary 96 ms). Nothing breaks at 3 years; what breaks is the trajectory, in precisely the class the repo itself defines as wrong and fixed everywhere else: `executive_overview`'s percentiles were moved to bounded SQL with the comment "برخلاف بقیهٔ این صفحه هیچ سقفی نداشت" (analytics.py:355-382), and `stage_stats` got `stage_stats_window_days` with the stated principle that a dashboard page's cost must not grow with the organization's whole history (`services/stage_stats.py:155-161`). my-scoring is the one analytic view left uncapped. How to reach: supervisor/deputy opens «آینهٔ ارزیاب من» (MyScoringPage) — every visit pays the full-history price, and it grows ~linearly with records × indicators. Fix: optional `period_id` query param (mirroring `_record_conditions` in reports.py) or reuse `settings.stage_stats_window_days`, applied to **both** subqueries so the "me vs org" comparison stays on the same window; default (no param) keeps today's behavior. One file + its parity tests.

No other finding survived execution — the details are in Verified correct.

## Verified correct
- **Evaluation list — the prompt's prime N+1 suspect is clean.** `GET /api/evaluations` is **4 queries constant** at every filter combination measured (plain page 1, deep `offset=6900`, `status`, `was_returned=false`, `on_ceo_desk=true`, `q=` ILIKE search, `seat_user_id`, `limit=200`): count-over-subquery, page, **one batched** `returned_ids` query for the whole page (evaluations.py:736-747 — the comment claims "یک کوئری دسته‌ای به‌جای N+1" and the line exists), current-user. `subject` and `hr_user` are `lazy="joined"` (models/evaluation.py:216-218), so `subject_full_name`/`hr_username` in `EvaluationRead` serialize with no per-row query. Deep offset 6900 = 26 ms.
- **Personnel list: 7 queries constant** at `limit=50` **and** `limit=1000` (16 ms / 78 ms) — `_with_accounts` batchs usernames, open records and access chains in 3 fixed queries (personnel.py:159-203). Improvement-plans list: 3 queries; detail uses `selectinload(goals)` where the serializer actually touches goals (improvement_plans.py:316).
- **Dashboard aggregates are genuinely SQL-side and test-locked.** `/dashboard/overview` = 10–12 queries, **one** scan of the 140k-row score table (the single-scan rule is enforced by `tests/test_dashboard_query_budget.py`), indicator text kept out of the `GROUP BY` so the `count(distinct)` cohort measure spills 2.5 MB to disk instead of 22 MB (reproduced: `Sort Method: external merge Disk: 2592kB`, 80 ms of the ~106–154 ms total). `pipeline` 2q/7ms, `period-trend` 2q/10ms, `expiring-contracts` 2q/9ms (correlated EXISTS served by `ix_evaluation_records_subject`), `role-overview` 1–5q. Fixed query count under data growth is enforced by tests, and the repo's `measure_hot_paths` output matches my harness within noise.
- **`stage_stats` cap regression holds**: `settings.stage_stats_window_days` default 1095 (config.py:157) applied at stage_stats.py:169-171; with `period_id` it's 2 queries/6.5 ms; unscoped it is 4 queries/**388 ms** — the Python-loop cost `docs/perf-3b.md` explicitly measured, documented and *decided not to change* (451 ms breakdown: 45 ms SQL, rest Python). Not re-reported; the decision and its escape hatch are written down.
- **Reports**: `/dashboard/report/summary` 6q/107 ms, `/indicator/{id}` 6q/59 ms, `/employee-vs-unit` 4q/10-25 ms — all aggregation in SQL; `export.xlsx` 9q/115 ms and correctly marked `EXPORT_ROW_CAP_NOT_APPLICABLE` (its sheets are bounded by org structure, not row count — the claim holds: 20 indicators, dozens of units).
- **Role analytics**: `/analytics/executive` 17q/25-49 ms — percentiles via `percentile_cont` in Postgres (the fix perf-3b documents), contract-exposure counts are 0.3 ms seq scans on a headcount-bounded table. `/analytics/my-scoring` 14q — see Finding 2 for its one gap.
- **Excel/PDF on the request path**: every export is row-capped at 5000 with the truncation note carried **inside the file** (design verified, cap+note via `cap_rows`); `applied_bonus_for_document` goes through `db.get(ScoringScheme, …)` (snapshot.py:37-54, scoring_scheme.py:100) so the identity map collapses it to ≤1 query per scheme, not per row — measured 5 queries for a 5000-row export. The official PDF is **archived once and served from bytes afterwards**: first render measured 381 ms including snapshot build, WeasyPrint render, hash, archive insert and audit event; archived serve 11.8 ms; `archive_final_pdf` is idempotent with a unique index on `evaluation_record_id` (documents.py:29-88), and concurrent first downloads collide safely on that index. Missing WeasyPrint libs → explicit Persian 500 (evaluations.py:1761-1769, code-verified).
- **Audit chain cost**: interactive `/audit-log/integrity` from the anchor is 3 ms-class (falls back to full only when no anchor exists yet — 723 ms at 25.5k rows, documented); full verification (1q, 717-733 ms) lives in the daily background sweep with a leader lock, never on a request path unless `?full=true` is requested explicitly. `audit_log` has indexes on `evaluation_record_id`, `event_type`, `created_at`; the `was_returned` filter and the stage-stats transitions query (`EXPLAIN`: 20 ms, hash join, in-memory sort) are properly served.
- **Indexes for every filter/sort the API ships** (compared model ↔ migration ↔ `\di`, EXPLAINed the suspects): evaluation_records has `status+created_at`, `created_at` (the perf-3b add — paged list plans as **Index Scan Backward, 0.1 ms**), `final_weighted_pct`, all four seat columns, subject, period, unique `verify_token`, partial open-objection, partial open-per-personnel; personnel has `org_unit`, `status`; personnel search ILIKE stays a 2 ms-class seq scan on a headcount-bounded table (the trigram decision in perf-3b, respected). The only unindexed filter columns live on tables bounded by headcount (`users.personnel_id`, `evaluation_access` seat columns — unique per personnel), where seq scans measure sub-millisecond.
- **Sweeps (background cost)**: SLA sweep **6 queries / 296 ms** at 7000 records — the 6251-query/4.5 s N+1 from perf-3b is still fixed; orphaned-cases 4q/45 ms, contract sweep 2q/2 ms, improvement-review 2q/2 ms.
- **Frontend bundle**: all 24+ routes are `React.lazy`; the heavy charts deps (recharts+d3) are isolated in `PersonCharts-*.js` = **381.36 kB raw / 108.66 kB gzip**, reachable only via `EmployeeProfileModal`/`PersonScorecard` (both lazy routes), never in the login path. Main chunk `index-*.js` 398.42 kB / **120.24 kB gzip**, react vendor 121.10 kB / 39.30 kB gzip, CSS 15.00 kB gzip; fonts are woff2-first (21 kB/subset). Page sizes are 10–20 rows (`DEFAULT_PAGE_SIZE` in EvaluationList.tsx:17, AuditLogPage.tsx:23, PersonnelPage.tsx:29) with server pagination, so no unvirtualized mega-lists. Background polling is deliberate and cheap: notifications every 5 s visible / 60 s hidden (queries.ts:342) against a 4-query/9 ms endpoint served by `ix_notifications_user_read`; the 15 s indicator poll only runs for open scoring forms. Query cache boundaries (staleTime, cross-user clearing) were P09's angle and hold.
- **ALREADY-FIXED cost regressions — none found**: (a) every Excel export builds the workbook **before** `commit` (evaluations.py:806-813, improvement_plans.py:355-359, audit_log.py:298-301, personnel.py, users.py) and eager-loads what its builder reads (`subject`/`hr_user` joined; `ImprovementPlan.personnel` joined, `goals` selectinloaded at improvement_plans.py:316); (b) `stage_stats` window cap in place; (c) Persian collation sort server-side via `core/sorting.persian_text` (measured 15-16 ms with sort); (d) cohort suppression counts people (`cohort_size` at every aggregate site).

## Could not check, and why
- **Multi-user concurrency / wire latency**: all numbers are in-process TestClient medians on a 2-core box — no load generator, no reverse proxy, no gzip-on-the-wire measurement. Query counts are machine-independent (that's the repo's own standard); absolute ms are not.
- **`my-scoring` at 10-year volume** (Finding 2's trajectory): extrapolated from the 3-year benchmark, not seeded — seeding 10× (1.4M score rows) was beyond a sane run on 2 cores. The linear-growth shape is visible in the query structure (full-table subqueries), not measured at that size.
- **WeasyPrint-missing production path**: the 500-with-Persian-message branch (evaluations.py:1761-1769) is code-verified only; WeasyPrint 63.1 is installed here so the happy path is what I measured.
- **PDF render time across documents of different length**: one record measured (381 ms); no sweep over snapshot sizes.
- **Full test suites / e2e**: not re-run per instructions (already green: 1542 backend, 407 frontend, oxlint/tsc/build).

## Proposals — only in `Mode: defects + elevation`, max 6, ranked

1. **A period selector for «نمای کلی» — HR can't answer "امسال چطور بودیم؟" without exporting.** The dashboard overview always aggregates the organization's entire history (today 140k score rows per view; it is the one dashboard page with no period filter, while reports and stage-stats already have one). Add the same optional `period_id` the reports accept to `/api/dashboard/overview` and one dropdown next to the existing site filter. Outcome: HR reads "this period vs all-time" on the page they actually open, and the landing page's cost stops growing with history. Files: `api/routers/dashboard.py`, `schemas/dashboard.py`, `api/queries.ts`, `pages/hr/DashboardPage.tsx` (4; default `None` keeps today's response shape, no migration).
2. **"Why is this case stuck?" should not re-download history — stream the full chain verification in keyset batches.** The nightly full verification loads every audit row ever written as ORM entities in one list (services/audit.py:199 and the full branch; 717 ms / 25.5k rows today, memory linear with org age) — fine now, hostile to a small VPS in year five. Walk `id > last` batches of a few thousand and fold the hash chain incrementally; semantics unchanged (the chain is sequential), anchor rules untouched. Outcome: the nightly job's memory and time stay flat however old the organization gets. Files: `services/audit.py` + its tests (2; no migration, no effect on signed documents — this reads the log, never writes it).
3. **Charts appear instantly when HR opens an employee profile.** Opening the profile modal on the personnel list fetches the 108.66 kB-gzip recharts chunk on first click, so the radar/trend pop in late on ordinary office links. Warm the chunk when the personnel page mounts (one idle-time dynamic `import("./PersonCharts")`) or hover-prefetch. Outcome: profile modal opens with its charts already rendered. Files: `pages/hr/PersonnelPage.tsx` (1).
4. **Show the export's row count before it's built.** The truncated-export note tells HR "با فیلترِ باریک‌تر دوباره بگیرید" only *after* a ~2 s blocked request (Finding 1's worst case). The page already knows `total` from the list query — surface "این خروجی N ردیف خواهد داشت" on the export button's confirm state, so HR narrows the filter first instead of waiting for the file to tell them. Outcome: fewer multi-second exports hitting the only HR worker. Files: `components/EvaluationList.tsx` (+2 list pages) (3).

---

# ══════════════════════════════════════════════════════════════════
# PROMPT 11 REVIEW
# ══════════════════════════════════════════════════════════════════

P11 — Test quality review (the test suite itself) · NexaHR @ 27c83e5 · Mode: defects

## Verdict — 3 lines
The suite is behavior-first, not mock-first: 1542 backend tests contain zero assertions on mocks, wall-clock handling is disciplined, and both former defects the prompt names (`af_race_*` pollution, `employee_view_on` hiding a default-off module) are verified to be in a **fixed** state. The real blind spot is breadth, not depth: five mutations I executed — the org-unit rename cascade, the org-unit delete guard, both notification-preferences guards, the admin delivery-queue problem list, and the public QR-verify page's `valid` flag — **all survived** (plus one control mutation that was caught, validating the harness). Highest-value gap: nothing tests that renaming an org unit moves its personnel, and `org_unit` is a string key behind bulk cohorts, the HR-unit shield and per-unit analytics.

## Findings — severity | file:line | what breaks | how to reach it | fix
Every "suite would not catch" claim below is backed by an executed mutation in /tmp/mutrepo (copy of backend@27c83e5, scratch DB `nexahr_p11`, 11-file subset baseline = 123 passed green; frontend full suite baseline = 407 passed green). Repo and shared DB untouched; both reverted/removed afterwards.

**1. Medium | backend/app/api/routers/org_units.py:138-144 | The rename→personnel cascade has no test — deleting it, the suite stays green (mutation MU-1 survived, 51 tests green).**
`update_org_unit` promises in its docstring that renaming a unit is a *correction* that moves the personnel rows whose `org_unit` string still carries the old name. That `db.query(Personnel)...update({...})` block is the only thing keeping personnel inside their unit after a rename. No test anywhere calls `PATCH /api/org-units/{id}` with `name`/`site` (grep over tests/: the only PATCH test is `test_hr_subject_chain.py:543-558` and it changes only `is_hr_unit`). Mutation: comment out the cascade → 51/51 green.
Reach: HR renames a unit in the admin panel (e.g. site added, or name typo fixed) → every member silently drops out of that unit on every string-keyed surface: bulk-create cohorts, the expiring-contracts watchlist, per-unit analytics — and, for an HR unit, the org-unit string that decides HR-unit membership (`tests/helpers.py:149-161` shows the string *is* the link). The audit event then reads `personnel_moved: 0` while HR believes the rename was complete.
Fix: one test that PATCHes a unit's name with two personnel rows in it and asserts both rows' `org_unit` followed, plus the audit event's `personnel_moved == 2`; and one test that a site-only change also moves rows.

**2. Low | backend/app/api/routers/org_units.py:158-197 | `DELETE /api/org-units/{id}` and its in-use guard are untested (MU-2 survived, 51 green).**
The endpoint refuses to delete a unit that has personnel (`if in_use:` at :178). Mutation: `if False and in_use:` → suite green; no test even mentions `DELETE` against `/api/org-units`.
Reach: a regression that drops the guard lets HR delete a unit with members; the catalog row disappears while `personnel.org_unit` strings keep referencing a unit that no longer exists in any form or list.
Fix: two tests — delete an empty unit → 204 and the row is gone; delete a unit with one member → 409 with the Persian count message and the unit still present.

**3. Low | backend/app/api/routers/notifications.py:82-131 | GET/PUT `/api/notifications/preferences` are untested — the address-required guards can be deleted with the suite green (MU-3 survived, 42 green).**
`update_preferences` is the only writer of `user.email`/`user.phone`, and its two 400 guards (:115-124) are what prevents a user from ticking "email/SMS" with an empty address — a tick that silently never delivers (the module-switch test's own docstring calls this shape "a tick with no effect"). Mutation: both guards → `if False and ...` → 42/42 green; grep: the word "preferences" appears in zero backend tests.
Reach: any authenticated user; PUT with `notify_by_email: true`, `email: ""` currently 400s — after the mutation it would persist the tick and the user waits for mail that never comes (the exact dead-queue shape `test_module_switches.py:252-291` warns about for the other half of the flow).
Fix: four tests — email-tick without address → 400; sms-tick without phone → 400; happy path persists address+flags and echoes `email_available`/`sms_available` from the configured channels; GET reflects them.

**4. Low | frontend/src/pages/VerifyPage.tsx:25-47 | The public QR-authenticity page has no test at all — making it declare every document valid passes the entire frontend suite (MU-6 survived, 407/407 green, full vitest run).**
`VerifyPage` is the one unauthenticated page that authenticates the signed employment document; its own comment (:37-38) says it must respect the server's `valid=false`. Mutation: `setStatus(data.valid ? "valid" : "invalid")` → `setStatus("valid")` → 61 test files, 407 tests, all green. No test file imports the component (grep), so no mutation against it can ever fail the suite by construction.
Reach: a refactor that swaps the ternary (or drops the catch branch) ships green; the page would show «این سند معتبر و نهایی‌شده است» plus the subject's name, unit and final score for any garbage/expired/unfinalized token. Backend 404/409 behavior is tested (`tests/test_documents.py:135-188`), but the last line of defense for a scanned printout is this component.
Fix: one vitest with a mocked axios: `valid: false` → the red «سند معتبری … یافت نشد» branch; `valid: true` → summary rows + SHA-256 shown, and the empty-hash «در حال آماده‌سازی» branch (:134-144).

**5. Low | backend/app/api/routers/admin.py:65-97 | `GET /api/admin/delivery-queue` is untested — hiding every failed delivery row stays green (MU-4 survived, 30 green).**
This is the operator's only answer to «چرا فلانی خبردار نشد؟» — the counts per `DeliveryStatus` and the 50 most recent failed/abandoned rows (:85-93). Mutation: `problems = []` → 30/30 green; grep: zero references in tests (it is one of only 4 backend routes — of 149 — never exercised by tests or e2e).
Reach: `view_diagnostics` holder opens the queue after an SMS outage; a regression that empties `recent_problems` (or mis-keys `counts`) reports a healthy outbox forever.
Fix: seed one failed + one succeeded `NotificationDelivery` row, assert `counts`, assert only the failed row is in `recent_problems` with `last_error`/`attempts` intact.

**On "tests that lock in a bug":** the only current candidate I could substantiate turned out to be sound on inspection — the bonus tests assert *raw* on the record and the *sum identity* on the snapshot, which forces `build_final_snapshot` to carry the applied value in the clamped case (`tests/test_review_round_one.py:170-188` asserts `base + bonus == final` with base 100). I therefore report no lock-in finding; the screen-shows-raw read schema is the repo's own open item (P03 also reported it) and no test pins it.

## Verified correct
- **The prompt's named example (`af_race_*` polluting the shared DB) is a FIXED state.** `tests/test_audit_fixes.py:578-706` still commits deliberately (two real connections need visibility), but cleanup now runs in a `finally` *before* the assertions (:665-703): the marker `Personnel` row (:693-700), `AiSettings`/`AiConversation`/`AiUserAccess` (`_cleanup_confirm_fixture` :394-413), and the M-10 `SchedulerRun` janitor (:771-780). Only the uuid-suffixed `af_race_*` *user* rows remain by design (append-only audit events reference them; documented in the code and in docs/open-findings.md «آلودگیِ مشروط رفع شد»). The conditional-pollution class is closed; the deliberate per-run user row is bounded to one per run.
- **Zero mock-assertion tests.** `MagicMock|assert_called*|Mock(` → 0 hits in 137 test files. The fake LLM (`tests/fake_llm.py`) is a scripted adapter; its wire capture (`ScriptedAdapter.wire`) is used to assert the *tool schema contract* (e.g. `tool_choice="none"` on the closing step) next to behavioral assertions, which is contract testing, not mock-testing.
- **Wall-clock hygiene.** `frozen_local_day` (conftest.py:79-103) freezes `now_local` at 12:00 local, away from both midnight boundaries; the designed midnight tests monkeypatch `now_local` deterministically (test_org_timezone.py:62-82). The only two `date.today()` sites (test_persian_messages.py:66, test_no_deputy_chain.py:249) build ±10-day margins and never assert window-open state, so no test's verdict depends on the hour it runs.
- **Committed-row hygiene.** Every test that writes through its own engine has fixture-teardown cleanup (test_score_write_lock.py:89-118 — including disabling the append-only trigger to delete its own audit rows, test_session_rotation_lock.py:44-57, test_improvement_plan_lock.py, test_scheduler_reliability.py:161-187, test_document_archival_failures.py:89-100, test_audit_fixes M-7/M-10). CI runs pytest serially (`scripts/ci-local.sh:55`, `pytest -q`), so the fixed-username committed fixtures (`lock_sup`, `rot_lock`, `P-ARCH`) cannot collide. Autouse rate-limiter reset (conftest.py:62-75) kills the ordering-dependent quota class; audit-existence assertions use filters/deltas, never global emptiness.
- **`employee_view_on` hides nothing anymore.** All 9 module keys in core/modules.py have default-off behavior tests (test_module_switches.py:74-291, including the two-switch coupling at :159-181 and the outbound-notify off-switch queue test), locked by `test_every_module_key_is_enforced_somewhere_on_the_server` (:297-315). The 8 files with `pytestmark = usefixtures("employee_view_on")` exercise the on-path; off-behavior coverage is never lost. `test_subject_result_gate.py:125` even tests the switch-off state explicitly.
- **Status-code-only class is bounded and mostly legitimate.** 829 status-code assertions across the suite; 110 test functions assert a 2xx without reading a body. I sampled 15: nearly all are 403/404/409 refusals (where the code *is* the behavior) or verify the effect through DB/helpers (test_no_deputy_chain.py:68-77 DB-checks `finalized` after the 200; test_vacated_seats_notice.py:77-88 checks the notification row's recipient and text; test_self_grant.py:46-50 checks the capability table). The genuinely unverified-success remainder sits in test_admin_reach.py:25-55 (creation status-only) — but each of those behaviors is body-verified elsewhere (role default caps at test_capabilities.py:140-152, personnel+account at test_personnel_with_account.py:31-48, chain PUT access exercised by dozens of suites), matching the repo's own open estimate (~8%, bodies checked elsewhere).
- **The audit-label lock is real.** test_audit_event_labels.py scans `event_type="..."` literals over all of app/ in both directions (written ⊆ labelled, labelled ⊆ written, :25-67), locks the frontend map keys *and texts* (:70-94), and bans string interpolation so the scan cannot go blind (:42-55).
- **Route coverage is 145/149** by literal-mention census over tests/ + e2e/; the 4 never-exercised routes are exactly findings 2, 3 and 5 (PATCH org-units is exercised but only for the flag — finding 1).
- **Mutation harness validated:** the control mutation (MU-5, `is_hr_unit` no-op in the same file) *was* caught — `test_hr_subject_chain.py::test_hr_can_flag_a_unit_from_the_panel` failed as predicted — so the surviving mutations above indicate absent tests, not a broken harness.
- Launcher suite green (89 passed, /tmp/launcher.log); backend 1542 green and frontend 407 green per the authoritative logs.

## Could not check, and why
- **Mutation runs were subset runs, not the full 7-minute suite.** For MU-1..MU-4 the 11-file subset (123 baseline-green) was used on scratch DB `nexahr_p11`; for each mutated surface, grep proves zero other test references it (org-units PATCH-with-name, org-units DELETE, "preferences", "delivery-queue"), so a full run could not change the verdict. MU-6 ran the *full* frontend suite. Scratch DB dropped; /tmp/mutrepo removed; `git status` clean.
- **Suite-count drift (prompt vs checkout).** The prompt cites 109 backend files/1131 tests, 47 frontend files/285 tests; this checkout has 137 test files/1542 backend tests and 61 frontend files/407 vitest tests (launcher 89 confirmed). Also the repo's own open-findings.md cites "27 of 330 status-only 2xx claims" — different counting basis than mine (110 of ~600 test functions; 829 assertion sites). Reported as drift, not findings.
- **docs/review-findings.md, docs/nafashr-port-candidates.md, docs/nafashr-reported-bugs-check.md do not exist** in this checkout (matches every other agent's note); I read docs/open-findings.md only after forming my view — its test-quality section agrees in kind with mine (status-only remainder "contained"; af_race_* user rows accumulate by design) and none of my five findings appear in it or in open-findings-ai.md.
- **Real-model copilot behavior and a real browser e2e** need an API key / a browser runner; outside this angle and already recorded by the P04 review.

---
